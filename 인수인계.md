# 인수인계 — 서아 타로봇 (스레드 @eslyn_yes)
최신화: **2026-09-26 새벽**. 다음 AI 는 이 맨 위 0절부터 읽는다. 자세한 건 22~25절.

> ## 0-0. 2026-09-26 오후 — 1턴부터 AI · 3턴 가림 없앰 · 판단은 인스타 DM (아래 0절보다 이게 맞다)
>
> - **스레드 답글은 1턴부터 AI(sonnet)가 쓴다.** 낱말 분류가 "시험관"을 "시험"으로 읽어 엉뚱한 답이 나간 사고 뒤 사장님 결정: 오인 사례를 규칙으로 다 못 막는다 → **이해는 AI, 막는 건 파이썬(검사).** 조립은 AI 세 번 실패 때 예비. 분류 AI 는 따로 안 부른다(`분류.분류(haiku써도되나=False)`, 유형은 카드 재료용).
> - AI 는 답글과 함께 `topic`·`sensitive`(건강·임신·병·죽음)·`wants_judgment`·`handoff` 를 낸다(`풀이.SCHEMA_답`). 한판 결과 `답["주제"/"민감"/"판단원함"/"넘김"]`.
> - **댓글 = 그림이 보여주는 데까지, 서아 판단(헤어져·연락해)은 안 함. 인스타 DM = 서아 생각을 던짐(유료, 100% 자동, 다른 채팅방에서 만드는 중).** **인스타 DM(유료) 안내는 3~4턴에 문맥 맞춰 한 번**(3턴은 흐름 맞으면, 4턴~ 아직이면 꼭 · 2턴은 너무 빠름). 안내엔 ① 궁금한 것 하나 살짝 ② DM 에선 더 깊게 ③ 인스타 DM 보내라. **댓글엔 유료·결제·가격 말 금지**(사장님 뒤집음 2026-09-26: "문지방 넘고 탈락하는 게 낫지 애초부터 탈락시키는 건 아니다" → 값은 DM 들어오면 DM 쪽이 결제 링크로 알림, `풀이.값말` 검사) — **문장은 안 주고 원칙만**(`풀이.안내법`). 사장님 설명 문장을 그대로 넣었다가 손님 답글에 그대로 나가서 격분 → `틀말` 검사. 안내는 답 맨 끝. 민감 주제·새 카드 턴은 안 함.
> - `손님기록.이번턴`: 3턴 가림 삭제. `넘겨도`(3턴~) · `꼭넘겨`(4턴~) · `안내함`. 넘긴 답은 그림="뒷면" 으로 적혀 두 번 안 넘긴다.
> - **대화는 5턴까지**(`손님기록.최대턴=5`), 6턴~ 답 안 함. **카드는 1턴 1장 + 더 보기 최대 2장**: AI 가 새 궁금증이 나오면 끝줄 "한 장 더 볼까?"(offer_card) → 손님 "응/봐줘" 면 다음 턴 새 카드 + **그림은 새 카드 턴만**. 낱말로 "딴 질문" 잡아 저절로 새 카드 까던 것·AI draw_more 는 없앰. 새 카드 턴엔 DM 안내 안 함(단 5턴은 함). 2턴~ 짧은 답("응 봐줘")은 안 버린다.
> - 반복 막기: 지난 내 답과 8글자 넘게 겹치는 줄은 `되풀이빼기` 로 **그 줄만 뺀다**(다시 쓰게 했더니 같은 카드 그림 설명이 계속 겹쳐 5턴 DM 턴이 조립으로 떨어졌다). "결혼까지 갔다 치고"·"니 탓 아니야"는 대화에 한 번.
> - 금지어(`엔진.BANNED`) 든 재료 줄은 AI 에게 안 준다(`풀이.금지빼기`, 카드말.json 에 "확실하게" "합격" "투자" "놓치면" 등 30군데) + 금지어 목록 한 줄을 `스레드판단` 에. 1턴·새 카드 턴·5턴엔 "한 장 더 볼까" 안 됨(offer_card 켜도 무시).
> - 5턴 뒤 "감사합니다" 같은 인사는 받는다. **인사 답글이 턴을 먹던 옛 버그**(발행·줍기가 종류를 안 넘김) 고침. 인사말에 예뻐·이뻐·예쁜·이쁜 추가.
> - 실제 댓글 모양(번호 1·2·3, 한 줄 재회 질문, 존댓말, "스하리 완!", 2턴~ 아주 짧음)으로 시늉함. 가짜 AI 점검 32개: 스크래치 check.py (세션 지나면 사라짐).
> - DM 안내 답글은 **공개**로 둔다 (사장님 결정 2026-09-26). 스레드 API 는 DM 기능이 없다(글·답글·프로필·통계·웹훅만). 인스타는 DM API 가 된다 → 스레드 댓글 → 인스타 DM 이 자동으로 되는 유일한 길. 가림(스포일러)은 안 씀. 공개라 구경꾼한테도 광고가 된다.
> - **답마다 두 모드**(사장님 2026-09-26): 치고 나가기(analysis, 그림·찍기, 100~400자) / 사람으로 받기(human, 그림·카드 말 금지, 30~200자, "사람 보는 눈"=서운함 쌓이는 박자·바라고 한 건 맡겨둔 것). AI 가 손님 말 보고 고름(`mode`), 1턴은 늘 치고 나가기. 이론 늘어놓기("세 종류" 등)는 `이론말` 검사. 규칙서는 `말투.SYS`(사장님 글 복붙 아님).
> - 1턴은 `말투.SYS_처음` + 되물음 끝맺기. 조립의 마지막조립(가림 문구)은 이제 안 쓴다.
> - 시늉 확인: 시험관 댓글 → 주제 "시험관 수정결과 기다리며 불안", 민감, 안 넘김. 연애 3턴 안 함 → 4턴 "...인스타 DM으로 오면 ... 더 깊게 봐줄게 거긴 유료야" 로 끝.
>
> ## 0. 2026-09-26 요약 — 지금 봇이 어떻게 도는가
>
> **한 줄로**: 78장 타로 · 카드 이름/수트/정역방향/카드 다루는 말 **전부 안 씀** · 손님 상황을 **찍어서** 말함 · 1~4턴 파이썬(토큰 0) · 5턴~ AI(규칙서 45줄 + 예시 2개) · 관계 말엔 "둘 아니면 아무도 몰라"+"끝까지 갔을 때를 떠올려 봐"+두갈래.
>
> ### 사장님이 이번 세션에 못 박은 규칙 (어기면 격분)
> 1. **손님은 타로를 모른다.** "소드 6 정방향" "컵 킹" "메이저" — 한 글자도 안 나간다. 그림은 답글에 붙으니 글은 "그래서 지금 니 상황이 어떻다"로 바로. (`엔진.check_output` 카드문법말, `카드말.json` 안에 "카드" 낱말 0개)
> 2. **카드 다루는 말도 안 쓴다.** "뽑았어" "덮어둘게" "그 카드 그대로" "거꾸로 나온 건" — 손님은 자기 손으로 안 뽑아서 기억에 없다. 얘기를 잇는 말로("아까 하던 얘기로 다시 갈게"). 번호 글("2번 뒤집었어")만 예외(손님이 직접 골랐다).
> 3. **타로는 찍는 거다.** 한 답에 찍기 2~3개(손님 성향·상대 마음·지금 상황) + "그림이 이렇게 나와서 그렇게 본 거야" + "니 탓만은 아니야" + 지금 어느 때 + 새 방향 물음. 팔자오빠(@paljaoppa) 답글이 본보기. 심문("대답 안 하고 넘어갔네") 금지.
> 4. **사람 관계는 둘 아니면 아무도 몰라.** 속상해서 한 말인지 진짜 끝 앞인지 카드가 못 정한다. 1턴 둘만줄, 2턴~ 먼미래줄("결혼까지 갔는데 지금 이게 그대로면 후회 막심 / 홧김에 놓고 후회하는 것도 똑같이"), 카드마다 두갈래("속상해서 한 말이면 A / 진짜 끝 앞이면 B"). 헤어질지 연락할지는 손님이 정한다.
> 5. **역방향은 "나쁘다"가 아니다.** "아직·속도 줄일 때·속에서만 도는 중" 결. 똑바로면 방향 말 자체를 안 한다.
> 6. **부호는 물음표만.** 마침표·쉼표 안 씀 — 한국 여자들 말투. 줄을 짧게 끊어서 읽히게.
> 7. **"지침" "갇힘" 같은 굳힌 명사 금지.** 사람이 말할 때 쓰는 말만. (`풀이.굳힌말` 검사)
> 8. **연애·재회·돈이 메인 주제** — 경우의 수가 많아야 한다. 카드마다 상대벌·돈벌·되물음 추가됨. 더 늘리려면 `카드말.json` 에 같은 꼴로.
> 9. **AI 규칙이 많으면 AI 가 병신 된다.** 규칙서 대신 예시. 금지는 코드 검사가 잡는다(`풀이.문맥답글` 문제 목록).
> 10. **이미지는 사장님이 보라고 할 때만** (전 세션이 그림 반복 요청으로 주간 한도 11% 날림). 토큰 아낀다. 사장님이 "고치러 갔니 보러 갔니" — **고치기 전에 이해한 걸 먼저 말하고 ㄱ 받는다.** 사장님 참고자료는 방식만 쓰고 문구는 안 베낀다.
> 11. 사장님이 준 실제 사고(손님 jjjin_a_): 3턴에 마음 털어놨는데 가림으로 등 돌림 · 다른 글에 달아서 1턴으로 되돌아감 → 둘 다 고침(25절).
>
> ### 데이터 (전부 `데이터/`)
> - `카드78.json` — 78장 기본(id·slug·ko·img·symbolPoints 옛것). 그림은 `../카드이미지/타로78장/`, R2 `tarot78/`.
> - **`카드말.json`** — 진짜 글감. slug → 정방향/역방향 → `뜻`(3) `그림`(4, 보이는 것→상황) `기울기`(쪽·줄3·쉽게) `되물음`(유형8, 연애·재회·돈은 4) `찍기`(3: 손님성향·상대마음·상황) `두갈래` `상대벌`(2) `돈벌`(2). 손으로 씀. AI 안 돌림. 검사 규칙: 카드문법 금지·부호 금지·한 방향 안 뜻/기울/쉽게 8글자 겹침 금지.
> - `반말뜻.json` `기울기.json` `카드별질문.json` — 카드말 없을 때 예비. 지금은 78장 다 카드말이 있어 사실상 안 씀.
> - 생성 스크립트는 세션 임시 폴더(`%TEMP%\claude\<세션>\scratchpad\ct_*.py`, `patch*.py`)에 있었고 세션 지나면 사라질 수 있음. **json 파일이 진짜다.**
>
> ### 코드 흐름
> - `타로엔진.digest()` — 카드말에서 뜻·그림·되물음·찍기·두갈래·상대벌·돈벌을 씨앗 고정으로 fact 에 실음. `check_output` — 카드 이름(뽑힌 것 포함)·카드문법말·똑바로 나왔 걸림.
> - `조립.py` 1~4턴 — 결(받아주기·되묻기·추측·짚기) × 칸. 1~2턴·마음말은 찍기 필수(유형별: 관계=상대, 일돈시험=성향, 불안결정=상황). 관계말이면 둘만/먼미래 + 두갈래 끼움. 돈 하소연이면 돈받기줄. `_겹침빼기` 로 같은 뜻 두 줄 방지. `쓴줄모으기(글아이디, 손님)` 손님 단위로 이미 나간 줄 피함.
> - `손님기록.py` — 턴 세기. **같은 손님 12시간 안이면 글 달라도 이어서**(`이어세는시간`). `마음말이냐`면 3턴 가림 미룸, `안내했나` 로 한 번만. `방금뒷면` → 돌아옴 여는줄.
> - `풀이.한판` — 가림(마지막)은 몇 턴이든 파이썬 조립. 5턴~ `문맥답글`(AI). 프롬프트: `말투.SYS`(45줄, 찍기·예시) + `톤(턴)` + `문맥프롬프트`(지난 대화·뜻·그림·찍어 볼 것·지금 어느 때·되물음·관계 블록) + `끝맺기`(남겨두기/단정/되물음 순환). 검사: 심문말·굳힌말·카드문법·부호·존댓말·미래단정. 게시글(무료·노하우·증상·한장)도 AI, 번호 글만 파이썬. `카드표기()`/`방향표기()` 로 AI 에게 이름 안 줌.
>
> ### 확인하는 법 (AI 호출은 토큰 씀 — 사장님 허락 뒤에)
> ```
> python 테스트.py                      # 67개 자가진단 (AI 없음)
> python 조립.py --시험                  # 1턴 답글 여러 개 (AI 없음)
> ```
> 10턴 시늉: `손님기록._읽기`/`창고` 를 메모리로 바꿔치기하고 `풀이.한판()` 을 턴마다 부른 뒤 `손님기록.기록하기()`. 5턴부터 sonnet 호출.
>
> ### 남은 것 / 알려진 한계
> - 카드는 랜덤이라 하소연에 밝은 카드가 나오면 어색할 수 있다(돈 찍기 "축하 못 받는 돈 자리야" 등). 실제 답글 보고 사장님이 짚는 카드만 고친다.
> - 뒷면·번호4장 그림은 옛 검정·금 그림체. 파스텔로 바꿀지 사장님 결정.
> - AI 5턴+ 가 거꾸로 뜻을 지어내는지, 심문조가 다시 나오는지 실제 댓글로 볼 것. 걸리면 `풀이.문맥답글` 문제 목록에 낱말 추가.
> - 카톡(`카톡/`)은 이번 78장·카드말 작업 **안 했다**. 스레드만.

---

(아래는 옛 머리말과 1~25절. 22~25절이 2026-09-25~26 작업이다)

# 타로봇 (서아 @eslyn_yes) — 인수인계

> **2026-09-25 새벽 정리.** 다음에 작업하는 AI는 **이 파일부터**, 그중 **0~3절 + 20절**을 먼저 읽는다.
> 4~19절은 만든 과정과 이유다. 지금 상태와 어긋나면 **20절이 맞다.** "옛것" 이라고 붙은 절은 읽지 않아도 된다.
> 그다음 `../타로기획안.md`.
>
> **승인 없이 아무것도 건드리지 않는다. 배포 금지(카드사 심사 중).**
> 사장님은 코드를 모른다 → 쉬운 말로 짧게. 조용한 오류가 제일 무섭다 → 실패는 텔레그램·빨간 불로 드러낸다.

---

## 0. 제일 중요한 것 — 팔자오빠와 100% 분리

**공용인 것은 페이스북(메타 앱) 하나뿐이다. 그 밖은 전부 따로다.** 사장님 지시: "다른 폴더·카테고리와 합치지 마라."

| | 팔자오빠 | 타로(서아) |
|---|---|---|
| 메타 앱 | `1731062281302338` | **같은 앱** (여기만 공용) |
| 스레드 계정 | @paljaoppa | @eslyn_yes |
| 스레드 토큰 | 금고(`토큰.enc`) | 창고 `seoa/토큰.json` + Secrets `SEOA_*`. **팔자오빠 칸에 절대 안 덮어쓴다** |
| 코드 | `유튜브업로더/쇼츠저장소/` (깃허브 insta-shots2) | `타로봇/` (깃허브 **tarot-seoa**, 공개) |
| 엔진 | 만세력 | 타로 |
| 기록 | R2 `threads-reply/` · `shorts/` | R2 **`seoa/`** |
| 깨우기 워커 | `shorts-wake` | **`seoa-wake`** |
| 말투 | 반말 남성 | 반말 여성 |

- 이 폴더는 **바깥 파일을 하나도 import 하지 않는다.** 표준 라이브러리 + boto3·pynacl·PIL 만
- `python 테스트.py` 가 그걸 **매번 검사한다** (엔진·분리·기록 잔재·R2). 바깥 이름이 한 글자라도 들어오면 실패
- ⚠️ `유튜브업로더/threads_token.py` 를 **그대로 돌리면 안 된다.** 팔자오빠 토큰을 덮어써서 그쪽 봇이 죽는다

---

## 1. 지금 상태 한눈에 (2026-09-25)

```
댓글 들어옴 ─ 타로 질문 → 1~4턴 조립(파이썬, 0원) → 3~15분 뒤 발행
           │             5~10턴 AI(클로드)      → 50~70분 뒤 발행 (AI 실패 → 조립이 대신)
           └ 칭찬·감사·사과·인사 → 채널관리(파이썬, 1시간마다)
글        ─ 새벽 4시 넘으면 하루치 4개 (한장·무료·한장·무료) 짜서 시각 되면 내보냄

누가 돌리나  PC 작업 스케줄러 `타로봇_바퀴` 5분마다 (심장박동 찍음)
            PC 꺼지면 → Cloudflare 워커 seoa-wake 가 20분마다 심장박동 보고 → 깃허브 seoa.yml 이 대신 한 바퀴
기록        전부 R2 창고 seoa/ (잠겨서). PC 에 기록 파일 없음. 깃허브에도 없음
알림        텔레그램 (팔자오빠와 같은 봇, 앞에 🔮 서아)
```

| 것 | 상태 |
|---|---|
| 스레드 발행·답글 | **돈다** (2026-09-24 실측 답글 30건/일, 5턴 넘는 손님 드묾 → 하루 값 거의 0) |
| PC 꺼졌을 때 대타 | **된다** (20절). Secrets 전부 들어감. 클로드 토큰만 아직 (20-6) |
| 토큰 60일 | **알아서 늘린다** (만료 20일 전부터). 막히면 텔레그램 |
| 웹 채팅 `웹/서버.py` | 코드 됨. PC 에서만. 결제 없음 |
| 카톡 챗봇 `카톡/스킬서버.py` | 코드 됨. PC 에서만 (cloudflared 터널). 18절·카톡/인수인계.md |
| 프로필 링크 | **비어 있다.** 3턴 "프로필에서 열어봐" 가 지금은 헛말 (사장님 몫) |
| 결제 | 값 안 정함 |

---

## 2. 파일

```
타로봇/
  자동.py            **본체.** 5분마다 한 바퀴 (하루치·답글·줍기·채널관리·토큰갱신). --대타 면 깃허브
  하루치.py          하루치 글 짜기·내보내기
  발행.py            스레드에 올리기, 댓글 긁기, 답글 만들기·내보내기
  채널관리.py        칭찬·감사·사과·인사에 AI 없이 답하기
  줍기.py            버려진 댓글 중 진짜 손님 되살리기
  타로엔진.py        카드·재료·검사
  조립.py            1~4턴 답글을 AI 없이 조립 (말결 4종)
  풀이.py            5턴부터 AI. 새 글도 여기. AI 실패 → 조립
  말투.py            AI 지침 (웹·스레드 공용)
  분류.py            댓글 유형·시제, 볼 댓글인지
  창고.py            **기록 전부 R2 seoa/** — 잠금·심장박동·암호(enc1)
  대기표.py          답글 대기표 고치는 유일한 문
  손님기록.py        손님별 턴·오간 말 (글 단위)
  발행기록.py        글 간격 3시간
  서아토큰.py        토큰 받기·확인·**자동 갱신**
  그림올리기.py      카드 그림 R2 tarot/
  성적.py            조회·댓글 재기
  알림.py            텔레그램
  테스트.py          점검 5가지 (전부 통과해야 한다)
  비밀올리기.py      깃허브 Secrets·워커 열쇠·저장소 공개 (사장님이 돌린다)
  반말뜻만들기.py 질문채우기.py   재료 한 번 만든 것. 다시 안 돌림
  워커/              Cloudflare 워커 seoa-wake (index.js, wrangler.jsonc)
  .github/workflows/seoa.yml   깃허브 대타
  데이터/            카드22 · 열린질문 · 반말뜻 · 글틀 · 답글틀 · 증상축 · 계정사실 · 그림주소 · 조사 문서
  기록/웹손님.jsonl  웹 채팅 손님만 (PC). 나머지 기록은 창고
  웹/서버.py 채팅.html          웹 채팅 (PC)
  카톡/              카카오 챗봇. 스레드 것 복사본 + 말투·말종류·스킬서버. **카톡만 따로 고친다** (카톡/인수인계.md)
```

깃허브 `koii0803/tarot-seoa` (공개, master). `기록/`·`카톡/기록/`·`카톡/비밀경로.txt`·`.bak` 은 안 올라간다.

---

## 3. 쓰는 법

**돌리기·보기**
```
python 자동.py --바퀴              한 바퀴 (PC 스케줄러가 이걸 5분마다 부른다)
python 자동.py --상태              스케줄러 상태 + 심장박동 + 창고 기록 뒤 8줄
python 자동.py --붙이기 / --떼기   스케줄러 등록·해제
python 창고.py --목록              R2 창고 파일 (잠겼나까지)
python 창고.py --보기 답글대기.json
python 테스트.py                   점검 5가지
python 서아토큰.py --확인          토큰 사는지 · 며칠 남았나
```

**깃허브·워커 (사장님 몫. 값은 안 찍힌다)**
```
python 비밀올리기.py               R2·텔레그램·서아 토큰 → Secrets
python 비밀올리기.py --클로드      claude setup-token 결과(클립보드) → Secrets   ← 아직 안 함
python 비밀올리기.py --워커        워커에 깃허브 열쇠
python 비밀올리기.py --공개        저장소 공개 (했음)
gh workflow run seoa.yml -R koii0803/tarot-seoa      깃허브 대타 손으로 한 번
```

**글·답글 손으로**
```
python 하루치.py --본다                 오늘 예약표
python 발행.py --내보내기               대기표 중 시각 지난 것 (시늉. --진짜 붙여야 나감)
python 풀이.py --한판 --who 손님 --test "댓글"   답글 하나 만들어 보기
python 채널관리.py / python 줍기.py    보기만 (--진짜 면 대기표에 올림)
```

**LLM 부르는 방식**
- `claude -p` 로 부른다. **구독으로 돈다. API 키 없음.** 깃허브에선 Secrets `CLAUDE_CODE_OAUTH_TOKEN`
- ⚠️ JSON 스키마 키는 영문이어야 한다. 한글 키(`글`)를 쓰면 API 가 400 을 낸다. `reply` 로 쓴다
- 검사에 걸리면 **최대 3번까지 다시 시킨다.** 3번 다 걸리면 조립이 대신 쓴다

---

## 4. 엔진이 내주는 재료 (`digest()`)

이게 LLM 에게 주는 **전부**다. 여기 없는 말은 글에 못 쓴다.

| 칸 | 무엇 |
|---|---|
| `중지` · `중지까닭` · `중지문구` | True 면 풀이를 안 쓴다. 까닭은 `나이` 또는 `무거움` |
| `나이밝힘` · `무거움` | 어느 쪽에 걸렸나 |
| `카드` · `카드번호` · `슬러그` · `로마숫자` | 뽑힌 카드 (사람+날짜 seed) |
| `방향` | 정방향 / 역방향 |
| `열쇠말` · `한줄` | 그 카드 요약 |
| `상징하나` · `상징뜻` | 그림에서 짚을 곳 한 군데 |
| `상징전부` | 그 카드의 상징 목록 (검사에 쓴다) |
| `질문유형` · `시제` | 연애·재회·일·시험·돈·관계·불안·결정 / 지난것·지금·앞일 |
| `손님단어` | 되비출 말. **원문에 있는 것만 남는다** |
| `열린질문` · `질문출처` | 되물음 한 줄 |
| `말투` | 반말(스레드) / 존대(사이트) |
| `앞면` · `앞면경로` · `앞면png` · `앞면웹` | 그림. **텍스트와 같이 나온다** |
| `뒷면` · `뒷면경로` | 뒷면 그림 |

### 심리 기술 네 개를 함수로 바꾼 것

| 기법 | 하는 일 | 어디 |
|---|---|---|
| 되비추기 | 손님이 쓴 말을 그대로 돌려준다 | `mirror_words()` · `verify_words()` |
| 투사 | 답을 주지 않고 그림을 먼저 보게 한다 | `pick_symbol()` |
| 열린 질문 | 단정 대신 물음으로 끝낸다 | `pick_question()` |
| 작은 한 걸음 | 오늘 할 수 있는 것 하나 | 사이트 글 ⑤항목 (아직 안 실음) |

---

## 5. 못 박은 규칙

### 카드 뽑기
- **`SALT = "paljaoppa-tarot-2026"`** — 바꾸면 모든 사람의 카드가 바뀐다. 쓰기 시작했으면 끝이다
- 카드는 **사람 + 날짜**로 고정. 같은 날 다시 뽑아도 같은 카드. 조작이 안 된다
- 2,200명 돌려서 22장 고루 나오는 것 확인. 정/역도 한쪽으로 안 쏠린다

### 안 받는 사람
- **나이가 나오면 그 자리에서 멈춘다.** 미성년만이 아니라 **전부**
  (`17살` `만 14세` `2010년생` `고2` `중학생` `스무 살` `나이` `몇 살` `서른` `3학년`)
  - `중지문구` 한 줄만 내보내고 그 사람에게는 더 답하지 않는다. 문구는 `STOP_LINE` 한 군데
  - 놓쳐서 받는 쪽보다 **막는 쪽으로 기울였다.** 성인도 나이를 쓰면 막힌다
- **무거운 신호**(죽고 싶다·자해 등)는 `무거움 = True`. **아무 말도 안 하고** 사람에게 넘긴다.
  되비출 말도 안 넘긴다. 우리는 상담사가 아니다

### 나온 글 검사 — 셋을 따로 부른다

`check_output(글, fact)`
- 금지어 · 미래 단정 · **다른 카드 이름** · **그 카드에 없는 상징** · 물음 없는 글
- 금지어는 사주 봇 목록을 **복사**해 왔다(import 아님).
  뺀 것 `죽을`(죽음 카드와 충돌) · `우울` `공황`(입력 감지 쪽으로 옮김).
  더한 것 `맞았` `맞힌` `예언` `운명입니다`
- 이름 오탐 두 개를 고쳐 뒀다 — 두 글자 이름(`달` `탑` `별` `힘`)은 뒤에 `카드`·방향이 붙을 때만 보고,
  뽑힌 카드 이름은 먼저 지우고 본다(`여황제` 안의 `황제`)

`말투검사(글, 말투)`

| 구덩이 | 잡는 말 |
|---|---|
| 말투 섞임 | 반말 글의 `입니다`·`세요`, 존댓말 글의 `뭐야`·`거야?` |
| 해설서 말투 | `의미합니다` `상징해` `나타낸다` |
| 상담사 말투 | `그랬구나` `많이 힘들었겠다` `토닥` |
| 점쟁이 말투 | `열리리라` `리로다` `천기` |
| 애교 | `ㅎㅎ` `ㅋㅋ` `ㅠㅠ` `~~` |
| 긴 문장 | 한 문장 45자 초과 |
| 이모지 | 3개 이상 |

**상담사 말투가 제일 위험하다.** 되비추기·열린질문을 시켰으니 LLM 이 그쪽으로 흐른다.

`글검사(글)` — 새 글에만. 조사에서 "이러면 죽는다"고 나온 것
- `스하리` `복채` `선착순` `마감` `카톡` `년차` `자격증` `무당` 같은 말
- 수량 제한(`20명만`), 링크

### 말투는 두 벌
- **스레드 = 반말**, 사이트 = 존댓말. 팔자오빠가 반말이고 스레드 타로리더도 전부 반말이라 맞췄다
- 반말에 **약간의 여성미.** 딱딱한 `뭐야?` 대신 `뭘까?`, 군데군데 `혹시`·`조금`. 애교는 안 쓴다
- 두 파일의 칸과 순서가 똑같이 맞춰져 있다. **한쪽만 고치면 안 된다**

### 글 간격 — 최소 4시간
- **올린 뒤 `발행기록.py --기록` 을 안 적으면 다음이 언제인지 모른다.** 반드시 적는다
- `최소간격시간 = 4` 한 줄만 고치면 간격이 바뀐다. `테스트.py` 가 4 아래로 내려갔는지 본다
- 왜: 짧은 간격으로 쏟으면 노출이 깎이고 봇 티가 난다. 조사에서도 몰아 올리는 계정이 좋아요 6이었다

### 그림
- 있는 곳 `카드이미지/타로22장/` — `00-fool.webp` … `21-world.webp` (PNG 도 같이)
- 뒷면 `카드이미지/타로-카드뒷면.webp` (이것만 아직 한글 이름)
- 파일 이름은 `카드22.json` 의 `img`·`imgPng` 에 박혀 있다. 따로 찾지 않는다
- 웹 주소 `webPath` 는 **파일 이름과 같다**(전부 영문으로 바꿔뒀다)
- `그림있나(fact)` 로 파일이 실제 있는지 본다. R2 올리기 전에 이걸로 거른다
- 그림을 새로 넣거나 이름을 바꾸면 `카드22.json` 의 `img` 를 다시 맞춘다

### 사이트 글을 고쳤을 때 `카드22.json` 다시 뽑기
```
saju-arcade 에서 npx tsx 로 TAROT_CARDS · TAROT_CONTENT 를 JSON 으로 찍어
타로봇/데이터/카드22.json 에 덮어쓴다 (처음 뽑을 때 이 방법으로 했다)
```

---

## 6. 계정 현황 — @eslyn_yes

| | |
|---|---|
| 이름 | **서아 타로보는여자** (이름 변경은 14일에 2번, 1번 씀) |
| 소개 | 🌙 하루에 한 장 / 🔮 댓글 남겨줘, 네 카드도 뽑아줄게 💗 / 🤍 몇 명만 받고 끝내지 않아, 복채도 안 받아 / ✨ 맞히는 자리 말고 같이 보는 자리 💖 |
| 팔로워 | 5명 |
| 글 | 1개 (2026-09-23 20:35, 힘 정방향) |
| 관심사 | 재테크만 남음. `타로` 추가는 입력칸이 안 먹어서 못 함 |

소개에 **조사에서 나온 우리 무기 두 개**를 박았다. 경쟁 계정은 전부 "20명만" 하고 마감하고, 전부 스하리를 복채로 요구한다.

---

## 7. 아직 안 된 것 (옛것 · 2026-09-23 — 지금은 1절·20-7)

| # | 할 것 | 비고 |
|---|---|---|
| 1 | **메타 토큰** | 초대·수락까지 됐는데 **장기 전환이 메타 500**. 9절 |
| 2 | ~~댓글에서 `질문유형`·`시제` 뽑기~~ | **됐다.** `분류.py` (13절) |
| 3 | 스레드 댓글 긁기·발행 | 팔자오빠 것을 **복사**해 고친다. import 금지 |
| 4 | 그림 붙은 답글 | **R2 올리기는 됐다(11절).** 남은 건 스레드에 그 주소를 넘기는 쪽 |
| 5 | 글감 미리 쌓아두기 | 4시간마다 하나씩 필요한데 지금 손으로 뽑는다 |
| 6 | 카드별 되물음 132줄 | `카드별질문.json`. 안 채워도 돌아간다 |
| 7 | 사이트에 그림 넣기 | `카드이미지/타로22장/*.webp` → `saju-arcade/public/img/tarot/` 복사만 |
| 8 | `sitemap.ts` 에 타로 44주소 | 기획안 7-3절 |
| 9 | `docs/이미지출처.md` 한 줄 수정 | 기획안 5-3절 |

---

## 8. 사장님이 정할 것 (옛것 · 2026-09-23)

1. **손님은 "될까요?" 로 묻는데 우리는 되묻는다.** 답글 세 갈래 중 뭘로 갈지
   (카드만 / 되묻기 / 둘다). 실제 글을 뽑아서 보여줬다. 내 추천은 **둘다**
2. 카드별 되물음 132줄을 채울지, 지금 유형x시제로 갈지
3. ~~되물음이 사람+날짜 고정이라 같은 사람이 두 번 물으면 같은 질문이 간다~~
   → **풀렸다.** 씨앗에 턴을 섞어서 턴마다 카드도 되물음도 달라진다 (12절)
4. 중지 문구(`STOP_LINE`)를 이대로 둘지
5. `돈`·`불안` 유형은 실제 댓글에 0건이었다. 표본이 한 글이라 안 지웠는데, 뺄지
6. 건강·임신 질문을 나이처럼 **바로 중지**로 막을지 (경쟁 계정들도 안 받는다)

**이미 정해진 것**
- 나이가 나오면 그 자리에서 중지 (미성년만이 아니라 전부)
- 질문유형 8개 (연애·재회·일·시험·돈·관계·불안·결정)
- 스레드 반말 · 사이트 존댓말, 반말에 약간의 여성미
- 글 간격 최소 4시간

---

## 9. 토큰 — **풀렸다 (2026-09-24 새벽)**

> **제일 짧은 길은 OAuth 가 아니라 "사용자 토큰 생성기" 였다.**
> 메타 앱 → 이용 사례 → Threads API → 설정 → 맨 아래 **사용자 토큰 생성기**
> → `eslyn_yes` 줄의 **[액세스 토큰 생성하기]** → 나온 토큰 복사 → `python 서아토큰.py --넣기`
>
> 허용 화면도, 앱 시크릿도, 브라우저 계정 바꾸기도 **필요 없다.** 아침 내내 막혔던 500 이 여기선 안 났다.
>
> 지금 상태: `@eslyn_yes` (29071491495768653) · 60일 토큰 · `~/.seoa_threads_token.json`
> 확인은 `python 서아토큰.py --확인`
>
> ⚠️ **페이스북 장기 토큰으로는 안 된다.** 스레드는 `graph.threads.net` 이라는 딴 물건이다.

### 아래는 막혔던 때 기록 (다음에 OAuth 로 받아야 할 때 참고)

| 단계 | 결과 |
|---|---|
| 앱 역할에 `eslyn_yes` → **Threads 테스터** 초대 | 됨 |
| 서아 계정에서 초대 수락 (스레드 → 설정 → 웹사이트 권한 → 초대) | 됨 |
| OAuth 허용 화면 | 됨 |
| 단기 토큰 | **나온다** |
| 장기(60일) 전환 | **메타가 500** |

```
ViewerContextCreationJustificationValidationException
```
단기 토큰으로 `/me` 조차 500이 난다. 토큰 자체가 아직 안 먹는다는 뜻이라
**수락이 메타 내부에 퍼지는 데 시간이 걸리는 것**으로 본다. 시간 두고 다시 하면 된다.

### 받는 법 — `서아토큰.py` 를 만들어 뒀다 (2026-09-23 밤)

```
python 서아토큰.py            새로 받는다
python 서아토큰.py --확인      가진 토큰이 사는지만 본다
```

팔자오빠 쪽 발급 절차를 **베껴 와서**(import 아님) 서아용으로 고친 것이다.
사장님이 하는 일은 **복사 두 번**뿐이다.
1. 메타 앱 화면에서 Threads 앱 시크릿 "보기" → 복사 (주소는 스크립트가 찍어 준다)
2. 브라우저에서 "허용" 누른 뒤 주소창 통째로 복사 (Ctrl+L, Ctrl+C). 404 페이지가 떠도 맞다

⚠️ **브라우저가 서아 계정으로 로그인돼 있어야 한다.** 팔자오빠로 로그인돼 있으면 팔자오빠 토큰이 나온다.
   그래서 마지막에 `/me` 로 계정 이름을 보고 **@eslyn_yes 가 아니면 저장하지 않고 멈춘다.**

받을 때 주의
- 장기 전환 주소는 `https://graph.threads.net/access_token` 이다. **`v1.0/` 밑이 아니다**
- 받은 토큰은 `~/.seoa_threads_token.json` 에만 넣는다. **팔자오빠 저장소에는 아무것도 안 쓴다**
- 토큰·시크릿 값은 화면에도 로그에도 안 찍는다. 길이만 말한다
- 페이스북 장기 토큰으로는 안 된다. 스레드는 `graph.threads.net` 이라는 **딴 물건**이고
  토큰도 스레드에서 따로 받아야 한다 (2026-09-23 확인)

---

## 10. 기록 (전부 2026-09-23)

- 폴더 만듦. 엔진·데이터·검사 작성
- 나이 중지 규칙. 실험 22건 — 나이 12건 전부 막힘, 멀쩡한 글 8건 전부 통과
  (`살이 쪄서` `세 번` `10세기` `3세대` 오작동 없음), 무거운 글 2건 사람에게
- 스레드 실측 → 댓글 23건에서 **재회 5건 · 시험 3건**이 나와 질문유형 추가 (6개 → 8개)
- 말투 두 벌 분리 + `말투검사()`
- `풀이.py` 로 실제 글 생성 성공. 실험이 검사기 오탐 두 개를 잡아냄 (`2달 반`의 달, `여황제`의 황제)
- 앞면 22장 도착 → 그림 이름을 카드 데이터에 붙여 **텍스트와 같이** 나오게 함
- 그림 파일 44개를 한글 → 영문으로 바꿈 (`00-바보.webp` → `00-fool.webp`). 뒷면은 안 건드림
- 계정을 **서아 타로보는여자** 로 바꾸고 첫 글 올림
- 토큰 — 테스터 초대·수락까지 됐으나 장기 전환이 메타 500 (9절)
- 스레드 타로 계정 **55곳 조사** → `데이터/스레드타로계정조사.md`
- 조사 결과를 글 생성기에 반영 — 글감 3종(한장·개업·무료) + `계정사실.json` + `글검사()`
- 소개글을 이모지 넣어 다시 씀. 글 간격 4시간을 `발행기록.py` 로 기계화
- 마지막 검사: `python 테스트.py` **전부 통과** (자가진단 67건 · 간격 · 분리 깨끗)
- **팔자오빠 쪽 파일은 읽기만 했고 하나도 고치지 않았다. 커밋·푸시 없다**
- (밤) **카드 그림 23장을 R2 에 올렸다.** `그림올리기.py` 새로 만듦. 11절

---

## 11. 카드 그림 R2 올리기 (2026-09-23 밤 · 됨)

스레드는 파일을 직접 못 받는다. **"이 주소의 사진을 올려줘"** 방식이라 그림이 인터넷 주소를 가져야 한다.
그 주소를 만드는 자리다.

### 한 것
- 뒷면 1장 + 앞면 22장 = **23장을 R2 에 올렸고 전부 공개 주소로 읽힌다(200)**
- 주소는 `데이터/그림주소.json` 에 적힌다. 밖에서는 `from 그림올리기 import 뒷면주소, 앞면주소`

```
python 그림올리기.py --확인            연결 시험 (작은 파일 올리고 읽고 지움)
python 그림올리기.py --올리기           23장 올리고 주소표 저장 (다시 돌려도 덮어쓰기만 함)
python 그림올리기.py --목록            지금 올라가 있는 것
python 그림올리기.py --주소 death      한 장 주소 보기 (13-death 로 물어도 된다)
```

주소 꼴 — `https://pub-71a067f62c754bb0b68600c1ca0758c9.r2.dev/tarot/card-back.png`

### 못 박은 것
- **PNG 로 올린다.** 스레드가 받는 건 JPEG · PNG 뿐이다. webp 는 안 받는다(사이트는 webp 그대로 쓴다)
- 버킷은 홈페이지 때 쓰던 공개 버킷(`babcheck-img`)을 같이 쓰되 **`tarot/` 접두어 아래만** 건드린다.
  올리기도 지우기도 이 밖으로 안 나간다. 팔자오빠 쇼츠(`saju-shorts/`)·기록과 안 겹친다
- 접근값은 윈도 사용자 환경변수를 **읽기만** 한다 (`R2_ACCESS_KEY_ID` · `R2_SECRET_ACCESS_KEY` ·
  `R2_BUCKET` · `R2_PUBLIC_URL` · `CLOUDFLARE_ACCOUNT_ID_2`). 파일에 안 적는다
- 올린 뒤 **공개 주소로 실제로 읽어 보고** 200 이 아니면 실패로 친다
- `boto3` 를 쓰므로 `테스트.py` 의 허용 목록에 `boto3`·`botocore` 를 더했다.
  pip 꾸러미지 이웃 폴더가 아니라 분리 원칙은 그대로다 (테스트 전부 통과 확인)

### 걸렸던 것 (다시 안 걸리게)
- **주소에 한글을 넣으면 안 된다.** 시험 파일을 `_시험.txt` 로 했다가 주소가 깨졌다. 열쇠는 영문만
- **r2.dev 앞 클라우드플레어가 이름표(User-Agent) 없는 요청을 403 으로 막는다.**
  브라우저·`facebookexternalhit`·curl 은 전부 200 이다. 메타가 긁어가는 건 확인했으니 문제 없다

### 스레드 그림 규격 (2026-09-23 공식 문서 확인)
| | |
|---|---|
| 형식 | JPEG · PNG (webp 안 됨) |
| 파일 크기 | 8MB 이하 — 우리 것 최대 1.9MB |
| 가로세로 비 | 10:1 이내 — 우리 카드 768×1376(1:1.79) 통과 |
| 너비 | 320~1440 — 앞면 768, 뒷면 1024 통과 |
| 주소 | 공개 서버여야 한다. 메타가 직접 긁어간다 |

### 다음 사람이 이어서 할 것
1. 답글에 그림 붙이기 — 스레드 2단계 발행에서 `media_type=IMAGE` + `image_url=뒷면주소()`.
   **토큰이 풀려야 시험할 수 있다(9절)**
2. 그림을 언제 앞면으로 언제 뒷면으로 보내는지는 **12절이 정한다** (2026-09-23 사장님 확정).
   기획안 4-4절의 "처음부터 뒷면" 은 이걸로 바뀌었다

---

## 12. 답글 3턴 규칙 (2026-09-23 사장님 확정 · 이게 기준이다)

한 손님과 주고받는 횟수로 그림이 달라진다.

| 턴 | 그림 | 하는 말 |
|---|---|---|
| 1턴 | **앞면** | 카드 보여주고 풀이 + 되물음 |
| 2턴 | **앞면** | 같음 |
| 3턴 | **뒷면** | "마지막 한 장" · 프로필에서 열어보라고 안내 |

### 왜 이렇게 정했나
- 처음부터 뒷면만 주면 손님이 받은 게 0 이라 그냥 나간다. **두 장은 그냥 준다**
- 3턴까지 온 사람은 진짜 궁금한 사람이다. **거기에만 보낸다.** 아무나 낚는 게 아니다
- 3턴째는 사이트가 **손님이 누군지 알 필요가 없다.** 가서 자기가 뽑으면 된다.
  (전에 막혀 있던 "사이트가 그 손님인 줄 어떻게 아나" 문제가 이걸로 사라졌다)
- 타로는 원래 3장 뽑는 자리라 "마지막 한 장" 이 말이 된다

### 안 바뀌는 것
- **답글에 링크를 넣지 않는다.** "프로필에서" 라고 말로만 한다. 봇 규칙 그대로다
- 3턴까지 안 오는 손님(대부분)은 그냥 거기서 끝이다. 쫓아가지 않는다

### 만든 것 (2026-09-23 밤 · 전부 돌려봤다)
| # | 무엇 | 어디 |
|---|---|---|
| 1 | 손님별 턴 세기 | `손님기록.py` · 기록은 `기록/손님기록.jsonl` |
| 2 | 역방향 그림 22장 | 180도 돌려 만들고 R2 에 올렸다. 정 22 + 역 22 + 뒷면 1 = **45장** |
| 3 | 3턴째 문구 | `풀이.py` 의 `"마지막"` 갈래 + `마지막검사()` |
| 4 | 댓글 하나 → 글 + 그림까지 | `풀이.py` 의 `한판()` |

```
python 풀이.py --한판 --who 손님아이디 --test "재회될까요" --type 재회 --tense 앞일
python 손님기록.py --볼것 손님아이디          이 사람 지금 몇 턴인가
python 손님기록.py --기록 손님아이디 --카드 "달 역방향" --그림 앞면
```

`한판()` 이 내주는 것 — 턴 · 답할까 · 글 · **그림주소** · 카드 · 방향.
1·2턴이면 앞면 주소(역방향이면 거꾸로 그림), 3턴이면 뒷면 주소가 나온다.

⚠️ **내보낸 뒤에 `손님기록.기록하기()` 를 반드시 부른다.** 안 적으면 영원히 1턴이다.
   `한판()` 은 일부러 기록하지 않는다 (안 나갔는데 적으면 턴이 어긋난다).

### 턴마다 카드가 달라지게 한 것 (8절 3번 문제도 이걸로 풀렸다)
카드는 사람+날짜로 고정이라 **그냥 두면 2턴에도 1턴과 똑같은 카드**가 나왔다(실제로 나왔다).
그래서 씨앗에 턴을 섞는다 — `손님#2` · `손님#3`.
조작이 아니다. 같은 사람·같은 날·같은 턴이면 늘 같은 카드다.
타로에서도 질문이 바뀌면 새로 뽑고, 2턴은 되물음에 답한 **새 질문**이다.
되물음도 같이 달라지므로 "같은 사람이 두 번 물으면 같은 질문이 간다" 던 문제도 사라졌다.

### 실제로 나온 글 (2026-09-23 밤, 검사 전부 통과)
```
1턴 [달 역방향 그림]   … 풀이 + 되물음
2턴 [황제 정방향 그림] … 풀이 + 되물음
3턴 [뒷면 그림]        3년 사귄 남자친구, 헤어진 지 2달 반.
                      이 카드도 뽑아놨어.
                      근데 이번엔 나도 여기서 안 열어.
                      프로필에서 열어봐.
```

### 아직 안 된 것
| # | 할 것 | 비고 |
|---|---|---|
| 1 | **프로필에 링크 걸기** | 사장님이 스레드 앱에서. **지금 안 걸려 있다.** 이게 없으면 3턴이 헛것이다 |
| 2 | 댓글 긁기 · 발행 | 토큰이 풀려야 한다 (9절) |

---

## 13. 댓글 분류 (2026-09-23 밤 · 됨)

엔진에 재료를 시키려면 **질문유형**(8개)과 **시제**(3개)가 있어야 하는데 지금까지 손으로 넣었다.
`분류.py` 가 댓글에서 뽑는다.

```
python 분류.py --시험                     실측 댓글 16건으로 맞나 본다
python 분류.py --댓글 "재회될까요"          한 줄 분류
```

### 돈 안 쓰는 순서 (팔자오빠 봇과 같은 생각)
1. 이모지·자모만·4글자 미만·링크 → **LLM 을 아예 안 부른다.** 답글도 안 만든다
2. 낱말로 잡히면 거기서 끝. **실측 댓글 16건이 전부 여기서 잡혔다 (16/16, 비용 0)**
3. 그래도 모르면 그때만 haiku. ("요즘 뭘 해도 마음이 붕 떠 있어" → 불안·지금, 맞음)

낱말표는 `분류.py` 의 `유형낱말` · `시제낱말`. 실측 댓글에서 나온 말만 넣었다.
**재회를 연애보다 위에 둔다** — 헤어진 뒤는 결이 다르고 실측에서 두 번째로 많았다.
`일`은 한 글자로 안 본다(앞일·내일에 다 걸린다). 덩어리로만 본다.

### 이제 `한판()` 은 유형·시제를 안 줘도 된다
```
python 풀이.py --한판 --who 손님아이디 --test "단절된 썸남에게 10월에 연락이 올까?"
```
실제로 나온 것 (2026-09-23 밤) — 연애·앞일로 잡고, 은둔자 역방향을 뽑고,
**역방향이라 거꾸로 된 그림 주소**가 붙었다. 검사 통과.

### 품앗이 댓글은 안 받는다 (2026-09-24, 실제로 들어와서 막았다)
첫 댓글이 `스하리` 였다. 질문이 아니라 거래 제안이다.
`스하리 · 맞팔 · 선팔 · 소통해 · 품앗이 · 홍보 · 디엠` 같은 말이 있으면 **LLM 도 안 부르고 넘긴다.**
조사 2부: 그렇게 모은 팔로워 4,861명짜리 계정이 좋아요 6이었다. 낄 판이 아니다.

---

## 14. 스레드에 올리고 댓글 긁기 — `발행.py` (2026-09-24 새벽 · 실제로 올라감)

```
python 발행.py --내글                    내 글 목록
python 발행.py --댓글                    내 글들에 달린 댓글 전부 (내 답글은 뺀다)
python 발행.py --올리기 일상 --시늉        글을 만들어 보여만 준다
python 발행.py --올리기 일상 --진짜        진짜 올린다
python 발행.py --답글 --시늉              댓글마다 답글을 만들어 대기표에 적는다 (안 나감)
python 발행.py --내보내기 --진짜           대기표에서 시각 지난 것을 실제로 단다
```

⚠️ **`--진짜` 를 안 붙이면 아무것도 안 올라간다.** 실수로 나가는 일이 없게 이렇게 뒀다.

### 올라가는 방식 (2단계)
1. 상자를 만든다 — `POST {내아이디}/threads` (media_type · text · image_url · reply_to_id)
2. 그 상자를 올린다 — `POST {내아이디}/threads_publish` (creation_id)

사진은 **메타가 그 주소로 직접 가지러 온다.** 그래서 그림이 R2 에 있어야 한다 (11절).
사진 상자는 만들고 8초 기다렸다 올린다. 바로 올리면 아직 안 받아갔을 수 있다.

### 글 창고 — 새벽에 만들어 두고 사람 많을 때 올린다
```
python 발행.py --쌓기 무료,한장,일상      미리 만들어 창고에 넣는다
python 발행.py --창고                    쌓인 것 보기
python 발행.py --창고올리기 1 --진짜       1번을 올린다
```
창고는 `기록/글창고.json`. 올린 것은 `상태: 나감` 으로 바뀌고 창고 목록에서 빠진다.

⚠️ **같은 날 만든 타로 글은 카드가 같다** (카드는 계정+날짜 고정).
   그래서 `무료` 와 `한장` 을 같은 날 둘 다 올리면 같은 카드 이야기가 두 번 나간다.
   **하루에 타로 글은 하나만.** 나머지 자리는 일상글로 채운다.

### 지키는 것
- 글과 글 사이 **4시간** (`발행기록.py` 가 막는다). 답글은 이 규칙에 안 걸린다
- 답글은 **4~37분 뒤 랜덤**으로 미룬다. 즉답은 봇 티가 난다
- 손님 한 명당 **3턴**까지 (12절). 4턴부터 답 안 한다
- 내가 단 답글은 다시 안 읽는다. 무한 대화 방지
- 대기표 `기록/답글대기.json` 에 상태(대기·나감·안함·실패)가 남는다

---

## 15. 일상글 (2026-09-24 사장님 지시)

> "타로 글만 올리면 안 된다. 일상글도 올려야 한다. 생초보 계정인데."

맞는 말이다. 타로 글만 있으면 장사꾼으로 보인다.
**다만 일상글은 반응을 노리는 게 아니다.** 조사 3부 ⑥: 타로 계정들 일상글은 전부 실패했다(좋아요 2~41).
일상글은 **계정이 사람으로 보이게 까는 바닥**이고, 반응은 타로 글로 받는다.

```
python 풀이.py --글 eslyn_yes --글감 일상
python 발행.py --올리기 일상 --진짜
```

소재는 `데이터/일상소재.json` 의 10개 축(자취·먹는것·잠·날씨·돈쓰기·몸·사람·혼자시간·고르기·쓸데없는생각).
**최근 6개에 쓴 축은 다시 안 뽑는다** (`발행기록` 의 `소재` 칸으로 센다).

### 일상글에만 거는 검사 (`일상검사`)
- **타로·카드·리딩·정방향·뽑았 같은 말이 하나라도 나오면 실패.** 여기서 너는 그냥 사람이다
- **숫자가 하나도 없으면 실패.** 실측에서 숫자가 사람 티를 냈다(월세 66만원, 새벽 3시)

실제로 올린 글 (2026-09-24 00:5x, 계정에 살아 있음)
```
장바구니에 담아만 두고 3주째 안 사던 원피스.
오늘 세일 떠서 결제했다.
39000원짜리가 19000원 됐길래.
근데 택배비 3000원 붙어서 결국 22000원.
세일이 세일이 맞나 이거.
```

---

## 16. 말투 손본 것 (2026-09-24 사장님 지시)

> "좀 여자여자하게 글도 쓰고, 전문가 댈 땐 전문가처럼 해야지"

`풀이.py` 의 `SYS` 에 두 덩어리를 박았다.

**여자가 쓴 글로 읽히게**
- 어미를 푼다. `~야` 로만 끝내지 말고 `~더라` `~네` `~같아` `~거든` 을 섞는다
- `뭐야?` 대신 `뭘까?`. 군데군데 `혹시` `조금` `살짝` `괜히`
- 한 군데쯤 혼잣말처럼 흘린다
- 단정해서 밀어붙이지 않는다. 가르치는 말투가 되면 실패
- 그래도 애교는 아니다. ㅎㅎ ㅠㅠ 는 여전히 금지

**아는 사람이 쓴 글로 읽히게**
- 몇 번 카드인지, 정·역방향인지 정확히 말한다
- 판단 근거는 반드시 **그림 안에 있는 것**. "느낌이 그래" "기운이 좋아" 는 아무나 하는 말이라 금지
- 역방향이면 뒤집혀서 뭐가 달라졌는지 한 줄 짚는다
- 그렇다고 강의하면 안 된다. 짧게, 툭 던지듯

### 아직 모르는 것 (정직하게 적어둔다)
되물었을 때 손님이 **답을 하는지 아무도 확인 안 했다.** 우리가 긁은 실제 댓글 23건은 전부 한 번 묻고 끝이었다.
답을 안 하면 2턴·3턴이 안 생기고 이 설계는 1턴짜리가 된다.
**글 하나 올려서 되물어보면 바로 안다.** 토큰 풀리면 이게 첫 번째 할 일이다.

---

## 17. 2026-09-24 낮에 바뀐 것 (⚠️ **19절이 이걸 뒤집었다.** 17-1 은 이제 틀렸다)

### 17-1. 제일 큰 변화 — **AI 를 위로, 기계를 아래로**

전에는 이랬다. 기계가 재료를 주고 AI 는 그 안에서만 문장을 다듬었다.
사장님 지적: **"기계한테 전권을 줬다 해도 문맥 파악 때문에 AI 의 직책이 훨씬 높다."** 맞는 말이다.

기계가 못 하고 AI 만 하는 것은 하나다 — **앞에 오간 말을 기억하고 잇는 것.**
카드 지식은 책에도 있지만 그건 AI 만 한다.

그래서 턴마다 길을 갈랐다.

| 턴 | 누가 | 시간 | 토큰 |
|---|---|---|---|
| **1턴** | **조립**(`조립.py`) | 0.0초 | **0** |
| 3턴 | AI (뒷면·프로필 안내) | 15초 | 씀 |
| 그 외 | **AI + 지난 대화 통째로** | 15초 | 씀 |

실측 댓글 23건이 **전부 한 번 묻고 끝**이었다. 1턴이 손님의 거의 전부다.
1턴을 조립으로 돌리니 토큰이 거의 안 든다.

**2턴부터는 지난 대화를 통째로 넘긴다.** 요약하지 않는다
(사장님: "한 손님 30분 붙잡아야 20~30턴인데 그 정도는 문제 아니다").

실제로 나온 것:
```
1턴 손님: 3년 사귄 전 남자친구랑 헤어진 지 2달 반인데 재회될까요
1턴 서아: (조립) 은둔자 정방향이야 혼자 있는 시간이 답을 꺼내주는 자리
2턴 손님: 그럼 내가 먼저 연락해도 돼?
2턴 서아: 먼저 연락하는 거 자체는 괜찮아
          근데 여사제가 두루마리 말고 있잖아
          그 사람한테 다 보여준 건 아니잖아
```

⚠️ **왜 전에는 문맥이 안 됐나** — 지난 대화를 넣어도 `SYS` 의 "재료에 없는 말은 한 글자도 쓰지 마라"
가 세서 AI 가 **지난 대화를 재료로 안 봤다.** 그래서 2턴용 얘기틀(`SYS_문맥`)을 따로 뒀다.
거기서는 1번 규칙이 "앞 대화를 이어라" 다.

### 17-2. 1턴 조립 (`조립.py`)
- 카드 44 × 되물음 704 × 상징 4~5 × 조립틀 12 = 수십만 가지. 판박이 안 된다
- 카드 뜻은 **반말판**(`데이터/반말뜻.json` 44줄)을 쓴다.
  엔진 뜻은 사이트용 존댓말이라 박으면 "달리고 있습니다" 가 된다. `반말뜻만들기.py` 가 한 번만 만들고 굳혔다
- **조사를 받침 보고 맞춘다.** "죽음가" → "죽음이"

### 17-3. 말투·형식 (사장님 지시)
| 항목 | 규칙 |
|---|---|
| 부호 | **물음표만.** 마침표·쉼표·이모지 전부 금지. `부호치우기()` 가 자동으로 지운다 |
| 길이 | 카드 글·답글 1~2줄 / 무료 4줄 / 개업 10줄 / 증상 4줄 |
| 결 | 실제로 잘 되는 계정 보고 맞춤 (아래) |

**2026-09-24 실제 계정 조사** — @i.m_yeontto(팔로워 1만)
- 좋아요 1.5천: "이게 사람사는 날씨냐 망고사는 날씨지"
- 댓글 105: "개백수라 시간많으니 타로 보고싶은 사람들은 질문들 하거라 간단한건 답해줌 ㅇㅅㅇ.."
→ **짧다. 안 다듬는다. 카드 설명을 길게 안 한다.** 잘 쓰려고 하면 망한다

### 17-4. 사고 막는 것 (팔자오빠에서 가져옴)
| 장치 | 내용 |
|---|---|
| **껍데기 차단** | 한글 20자·전체 30자 미만이면 버린다. `test reply` `ok` `네` 전부 막힘 |
| **하루 상한 998** | 메타 한도 1,000 에서 2 남김 |
| **텔레그램** | 막힌 답글이 있으면 알린다. 팔자오빠와 같은 봇, 앞에 🔮 서아 가 붙는다 |
| 품앗이 거르기 | 스하리·맞팔·소통 → LLM 도 안 부르고 넘김 |

> 팔자오빠는 2026-09-23 밤 손님 댓글에 **"test reply"** 를 발행했다.
> 검사를 잠깐 껐다가 뚫렸고 **API 로 답글을 못 지워서** 사장님이 앱에서 손으로 지웠다.
> 나가기 전에 막는 게 전부다.

### 17-5. 유입 설계 바꾼 것
| 항목 | 전 | 후 | 왜 |
|---|---|---|---|
| 무료 리딩 | 하루 1개 | **2개** | 댓글 붙는 건 이것뿐 |
| 턴 제한 | 3턴에서 자름 | **10턴** | 대화가 길수록 스레드가 밀어준다 |
| 프로필 안내 | 3턴에 자르며 | 3턴에 **한 번만**, 그 뒤 계속 답함 | 두 번 하면 영업 |
| 답글 지연 | 4~37분 | **3~15분** | 빨리 답해야 글이 다시 올라간다 |
| 기계 바퀴 | 10분 | **5분** | 같은 이유 |
| 일상글 | 하루 2개 | **뺌** | 조회 5. 타로 글의 1/38 |

### 17-6. 그림 — **항상 정방향** (사고였다)
역방향이라고 180도 돌린 그림을 올렸더니 **제목 글씨까지 뒤집혀** 업로드 사고로 보였다.
하필 매달린 사람이라 거꾸로 매달린 그림이 똑바로 서 버렸다.
**역방향은 글로만 말한다.** 돌린 22장은 R2 에 그대로 두고 `역방향그림쓰기 = False` 로 꺼 뒀다.

### 17-7. 새로 생긴 파일
| 파일 | 하는 일 |
|---|---|
| `조립.py` | 1턴 답글을 AI 없이 조립 |
| `반말뜻만들기.py` | 카드 뜻 44줄 반말로 굳히기 (한 번만 돌림) |
| `성적.py` | 조회·좋아요·댓글·리포스트 모아 `기록/성적.csv` |
| `알림.py` | 텔레그램 |
| `자동.py` | 윈도 스케줄러가 부르는 자리 |
| `하루치.py` | 새벽 4시 하루치 짜기 + 시각 되면 내보내기 |
| `발행.py` | 실제 발행·댓글 긁기 |
| `분류.py` | 질문유형·시제 뽑기 + 품앗이·인사 거르기 |
| `데이터/증상축.json` | 증상 훅 22축 × 가르는 말 3 × 글꼴 5 |
| `데이터/글틀.json` · `답글틀.json` | 글·답글 형식 각 10개 |
| `데이터/반말뜻.json` | 카드 뜻 반말 44줄 |

### 17-8. 첫 성적 (2026-09-24)
| 조회 | 댓글 | 글감 | 훅 |
|---|---|---|---|
| 192 | 1 | 한장 | 나 오늘 힘 카드 뽑았어 정방향 |
| 60 | 1 | 한장 | 나 오늘 매달린 사람 뽑았는데 역방향이더라 |
| 5 | 0 | 일상 | 장바구니에 담아만 두고 3주째 안 사던 원피스 |

### 17-9. 깃허브 (옛것 — 20절)
`koii0803/tarot-seoa` (비공개) 만들고 올렸다. `기록/` 은 안 올린다(손님 개인정보).
⚠️ 깃허브가 대타로 돌려면 기록을 **R2 로 옮겨야** 한다. 팔자오빠가 그렇게 한다. **아직 안 했다.**

### 17-10. 다음 사람이 할 것 (옛것 — 20-7)
| # | 할 것 | 왜 |
|---|---|---|
| 1 | **웹 채팅** | 스레드는 공개라 깊은 얘기를 못 한다. 돈은 여기서 난다 |
| 2 | ~~카톡 챗봇~~ | **스킬 서버는 됐다 → 18절.** 오픈빌더 승인만 남았다(사장님) |
| 3 | 기록을 R2 로 | 깃허브 대타가 돌려면 필요 |
| 4 | 20초 넘으면 조립으로 대체 | 카톡 1분 제한 대비 |
| 5 | 프로필 링크 | 사장님 몫. 없으면 3턴 안내가 헛말 |

---

## 18. 카톡 챗봇 스킬 서버 (2026-09-24 낮 · 코드는 됨, 승인은 사장님 몫)

사장님 지적: **"카톡 안 만듦?"** 맞다. 밤새 웹만 만들고 카톡을 건너뛰었다.
그래서 `카톡/스킬서버.py` 를 만들었다. **엔진·조립·풀이는 웹과 똑같은 걸 쓴다.** 기록만 따로다.

### 18-1. 시간 두 개를 피해 간다

| 카카오가 정한 것 | 여기서 어떻게 |
|---|---|
| 첫 답 **20~25초** (2026-09-24 사장님 확인) | 1턴은 0.04초 즉답. 2턴부터는 0.01초에 "카드 뽑는 중" |
| 콜백 **1분** | 45초에 끊고 조립글로 밀어넣는다. 빈손으로 안 끝낸다 |

| 턴 | 어떻게 | 실측 | 토큰 |
|---|---|---|---|
| **1턴** | 조립 + 카드 그림, 그 자리에서 답 | **0.04초** | **0** |
| **2턴~** | "카드 뽑는 중" 먼저 → 뒤에서 AI → 콜백 | 즉답 **0.01초** / 콜백 **14.0초** | 씀 |

### 18-2. ⚠️ 여기서 한 번 데였다 — 답하기 전에 하이쿠를 불렀다

처음엔 답을 내보내기 **전에** `재료()` 를 부르게 짜 놨다.
그 안의 `분류.분류()` 는 낱말로 안 잡히면 **하이쿠를 부른다.**
그래서 `"카드 뽑는 중"` 한 줄 내보내는 데 **11.76초**가 걸렸다. 시험에서 잡혔다.

고친 것 두 가지.

1. **콜백 쓰는 턴은 답하기 전에 아무 일도 안 한다.** 카드 뽑기·분류를 전부 콜백 스레드로 옮겼다
2. **1턴은 하이쿠를 끈다** (`재료(..., 하이쿠=False)`)

> 낱말만으로 실측 댓글 **16/16 맞음, 0.00초** (`python 분류.py --시험`).
> **17절에서 "1턴은 토큰 0" 이라고 적었는데 사실이 아니었다.** 분류가 하이쿠를 부르고 있었다.
> 카톡은 이제 진짜 0이다. ⚠️ **웹(`웹/서버.py`)은 아직 1턴에도 하이쿠를 부른다.** 거기도 끄면 된다

### 18-3. 쓰는 법

```
python 카톡/스킬서버.py                      열기 (http://localhost:8766/skill)
python 카톡/스킬서버.py --길 /skill/abc123    주소 숨기기 (아무나 못 찍게)
python 카톡/스킬서버.py --시험 "재회될까"      서버 안 열고 답만 찍어본다
```

`GET /` 로 살아 있나 본다. 오픈빌더는 `POST` 만 쓴다.

### 18-4. 카카오가 주고받는 꼴

받는 것 — `userRequest.utterance`(손님 말), `userRequest.user.id`(손님 구분),
`userRequest.callbackUrl`(콜백 주소).

보내는 것 — 바로 답할 때
```json
{"version":"2.0","template":{"outputs":[
  {"simpleText":{"text":"죽음 역방향이야\n..."}},
  {"simpleImage":{"imageUrl":"https://pub-....r2.dev/tarot/13-death.png","altText":"뽑은 카드"}}]}}
```
기다리라고 할 때 — `{"version":"2.0","useCallback":true,"data":{"text":"카드 뽑는 중"}}`
그 뒤 같은 `template` 을 `callbackUrl` 로 POST 한다.

### 18-5. 실제로 오간 말 (시험, 2026-09-24)

```
1턴 손님: 3년 사귄 남자친구랑 헤어진 지 두 달인데 재회될까
1턴 서아: (조립 0.04초) 사귄 남자친구 하는 마음에 세계가 정방향으로 떴네
          거의 다 왔어 여기서 멈추는 게 실패야
2턴 손님: 그럼 내가 먼저 연락해도 돼?
2턴 서아: (AI 14초) 세계 정방향 뜬 거 보고 바로 연락부터 물어보네
          근데 오늘 나온 건 교황 역방향이야
```
**2턴이 1턴 카드를 받아서 잇는다.** 문맥이 실제로 넘어간다.

### 18-6. 승인 나면 사장님이 할 것 (이 순서대로) — 카톡/인수인계.md 가 최신

| # | 할 것 | 어디서 |
|---|---|---|
| 1 | 오픈빌더 신청·승인 | 카카오 i 오픈빌더 (며칠 걸림) |
| 2 | 스킬 만들고 **"AI 챗봇 콜백" 켜기** | 오픈빌더 → 스킬 |
| 3 | 스킬 주소칸에 **밖에서 닿는 https 주소** 넣기 | 오픈빌더 → 스킬 |
| 4 | 시나리오 폴백블록에 이 스킬 연결 | 오픈빌더 → 시나리오 |

⚠️ 3번이 걸림돌이다. 지금은 집 PC 라 밖에서 못 닿는다.
콜백을 안 켜면 `callbackUrl` 이 안 와서 2턴부터도 조립으로 답한다 (늦지는 않는다).

### 18-7. 카톡에서 아직 안 한 것 — 카톡/인수인계.md 6절이 최신

| 것 | 왜 |
|---|---|
| 결제 | 값을 안 정했다. 지금은 "여기까지가 무료야" 문구로만 막는다 |
| 3턴 프로필 안내 | 프로필이 비어 있다. 지금 넣으면 헛말이다 |
| 밖에서 닿는 주소 | 3번 걸림돌. 도메인·터널 정해야 한다 |


---

## 19. 2026-09-24 밤 — 글 만드는 구조 (1~4턴 파이썬, 5턴부터 AI). **돌리는 구조는 20절**

앞 절(17·18)과 어긋나면 **이 절이 맞다.** 그날 하루에 두 번 크게 뒤집혔다.

### 19-0. 한눈에

```
댓글 들어옴
 └ 타로 질문인가?
    ├ 예 → 1~4턴   조립(파이썬)   토큰 0      3~15분 뒤 발행
    │      5~10턴  AI(클로드)     약 5,800    50~70분 뒤 발행
    │              └ AI 가 실패하면 조립이 대신 쓴다
    │      11턴~   답하지 않는다
    └ 아니오
       ├ 칭찬·감사·사과·인사 → 채널관리(파이썬)  토큰 0  1시간마다
       └ 아무 데도 안 걸림 → 안 답한다

버려진 것(안함·실패) → 줍기가 5분마다 회수
```

### 19-1. 17절을 뒤집었다 — **AI 를 위가 아니라 뒤로**

17절에 *"AI 를 위로, 기계를 아래로"* 라고 적혀 있다. **그건 이제 틀렸다.**

사장님 지시 (2026-09-24 밤): **"나는 스레드 댓글 AI 안 쓸 거야. 파이썬만 할 거라고. 그러면 0원이잖아"**
그 뒤 **"5턴부터 AI 사용으로 변경해"**.

왜 이게 되는가. 타로는 사주와 다르다.

| | 사주 | 타로 |
|---|---|---|
| 재료 | 여덟 글자·오행·대운·세운 — **서넛을 엮어야** 말이 된다 | **카드 한 장 뜻만 말하면** 문장이 된다 |
| 경우의 수 | 518,400 (유한하지만 엮기가 안 된다) | **44칸** (22장 x 정역) |
| 미리 만들기 | 못 한다 | **된다** |

그래서 팔자오빠(사주)는 1턴부터 AI 지만, 서아(타로)는 4턴까지 파이썬으로 덮인다.

**바꾸는 자리는 `풀이.py` 의 `AI안쓰는턴 = 4` 하나다.**

### 19-2. 조립이 사람처럼 말하게 만든 것 — **말결 4종**

전에는 조립이 매번 같은 순서로 쌓고 **매번 물음표로 끝냈다.** 틀이 보인다.

사장님 지시: *"100% 가 아니라는 거야. 어떨 때는 의문형으로 물어보기도 하고 공감도 했다가,
어떨 때는 어느 정도의 예측도 했다가 (상대방의 마음). **사람이랑 대화를 해야 하는 것이 첫 번째,
배설의 들어줌을 하는 것이 두 번째야.**"*

`조립.이번결()` 이 턴마다 결을 바꾼다. 결마다 켜는 칸과 길이가 다르다.

| 결 | 뭘 하나 | 길이 | 언제 |
|---|---|---|---|
| 받아주기 | 공감만. 답을 안 준다 | 55~95자 | **3턴부터** (1~2턴 공감은 영혼 없어 보인다) |
| 되묻기 | 짧게 받고 물음으로 넘긴다 | 55~95자 | 늘 |
| 추측 | 상대 마음을 찍어 던진다. **단정 안 한다** ("이런 거 아닐까") | 95~150자 | 연애·재회·관계만 |
| 짚기 | 안 적은 걸 짚고 한 마디로 박는다 | 95~150자 | 늘 |

**추측이 틀려도 된다.** 틀리면 손님이 고쳐 말하는데 **그 고쳐 말하는 게 다음 배설이다.**

### 19-3. 조립이 쓰는 칸 (순서 고정)

무작위로 고르면 앞뒤가 안 맞는다. 순서는 고정이고 **켤지 말지만** 결이 정한다.

```
여는줄 → 짚기 → 공감 → 추측 → 상대속마음 → 그림 → 뜻 → 뒤집기 → 완충 → 자기얘기 → 모른다 → 끝
```

- **여는줄** 2턴부터는 "아까 그 카드 그대로 보고 있어" 로 **앞 카드를 이어받는다.** 새로 뽑은 척하면 "아까 그거잖아" 가 된다
- **뜻** 1턴과 짚는 턴만. 같은 카드라 매번 쓰면 앵무새가 된다
- **끝** 1턴은 되물음 고정(대화를 열어야 하니까). 2턴부터 남겨두기 → 단정 → 되물음

### 19-4. 되비춤이 깨지던 것 — 범인 셋

손님 말을 되비추는 자리에서 이런 게 나왔다.

```
손님: 남친이랑 헤어졌는데 다시 연락올까
봇  : 남친이 헤어졌는데 하는 마음에 죽음이 역방향으로 떴네     ← 말이 안 된다
```

| 범인 | 고침 |
|---|---|
| `타로엔진` 조사 목록에 **`이랑` 이 없었다.** `랑` 만 있어서 "남친이랑" → "남친이" | `이랑` 넣고 긴 것부터 보게 |
| `[가-힣]{2,}` 로 긁어서 **"헤어졌는데" 같은 용언 어절이 명사인 척** 꽂혔다 | `명사낱말()` — **사전 방식.** 추측 안 한다 |
| **틀이 명사형·절형 두 종류인데 되비춤은 한 종류만 만들었다** | `절뽑기()` 새로 만들고 틀을 짝지었다 |

**셋째가 제일 컸다.** 절반은 반드시 깨지고 있었다.

`절뽑기()` 는 **안 자른다.** `-는데 / -거든 / -잖아 / -니까 / -아서` 로 끝나는 데까지 통째로 가져온다.
자르니까 깨지는 거라 안 자르면 문제가 애초에 안 생긴다. (`-까` 는 뺐다 — 넣으면 질문 전체가 잡혀 앵무새가 된다)

**전수 검사 240건 · 82~177자 · 평균 125자 · 깨짐 0 · 금지어 0 · 중복 줄 0.**

### 19-5. 타로 계정 실측에서 배운 것 (@awon_tarot, 댓글 139개)

| 배운 것 | 어떻게 넣었나 |
|---|---|
| **139개 답변에 카드 이름이 하나도 없다** | 카드줄을 1턴에서만, 그것도 길이 모자랄 때만 켠다. 화면에 그림이 이미 있다 |
| 제일 길고 공들인 게 **"남자친구 속마음"** 건이었다 | `속마음줄` — 연애·재회·관계일 때만 |
| 나쁜 소식 뒤에 **바로 완충을 붙인다** | `완충줄` — 역방향일 때 |

> 원문: *"연락운 자체는 낮은데 **잠수 탈 가능성은 낮으니** 일단 조금더 기다려보쟈"*

**전환율 시장 신호도 봤다.** "무료타로 하면 유료로 이어지나요" 글(댓글 32개)에서
제일 많이 눌린 답은 **"무료에서 유료로 전환되는 경우는 극히 드뭅니다"** (좋아요 13, 나머지는 1~2).

### 19-6. 5턴부터 AI — **파이썬이 못 하는 것만 시킨다**

`풀이.SYS_문맥`(2~3줄짜리 옛것)을 **지웠다.** 지침을 `말투.py` 하나로 모았고 **웹이랑 같이 쓴다.**

그 전에는 **5턴까지 온 제일 깊은 대화가 제일 낡은 지침으로 돌고 있었다.** 거꾸로였다.

AI 에게 시키는 것 — 전부 규칙으로 못 하는 것들이다.

```
가. 어긋남을 짚어라        "그쪽이 먼저 헤어지자고 했어" + "두 달째 생각나" 사이
나. 오래된 실을 되살려라   세 턴 전에 흘린 말을 지금 꺼낸다
다. 피한 걸 알아채라       물었는데 답 안 하고 넘어간 것. 다시 묻지 말고 피했다는 사실을 말한다
라. 고쳐 말한 걸 받아라    "아니야 그건" 하면 변명 말고 거기서 다시 출발
마. 카드와 손님 말을 한 문장으로 묶어라
바. 말의 온도를 읽어라
```

**하지 말라고 박아 둔 것** — 카드 뜻 설명 · 상징 나열 · 손님 말 인용 · "이 카드로는 안 보여" 류 ·
"나도 이 카드 보면" 류 · 카드 이름 또 부르기. **1~4턴에 파이썬이 이미 다 했다.**
그걸 또 하면 값만 나가고 손님한테는 같은 말이 두 번 간다.

실제로 나온 5턴 (실측):

```
손님  그럼 그냥 기다리라는 거야
서아  기다리라는 말은 내가 한 적 없는데            ← 어긋남
      그 말 니 입에서 나온 거잖아
      기다리는 거보다 안 움직이는 게 덜 아파서
      그렇게 말한 거 아닐까 싶기도 하고            ← 추측 (단정 안 함)
      아까 그 손 든 카드는 그대로 두고 있어         ← 오래된 실
      프로필에서 열 수 있다던 그 장은 아직 안 열었어  ← 3턴을 회수
      거기 열면 정해뒀던 답이 흔들릴 수도 있어서
      지금은 그냥 둔다                             ← 남겨두기로 닫음
```

**3턴에 파이썬이 깔아놓은 실("프로필에서 열 수 있어")을 5턴에 AI 가 주워 올렸다.**

### 19-7. AI 가 실패하면 조립이 대신 쓴다

| AI 쪽 사고 | 전 | 후 |
|---|---|---|
| 검사에 세 번 걸림 | 답 안 나감 | **조립이 대신 씀** |
| 시간 초과 | 답 안 나감 | **조립이 대신 씀** |
| 사용량 한도 | 답 안 나감 | **조립이 대신 씀** |

실제로 `@seyraen_ae` 가 **8시간을 기다렸다** — AI 가 쓴 답이 `애교: ㅋㅋ` 로 세 번 다 걸려서.
대신 쓸 때마다 텔레그램으로 알린다. 조용히 넘어가면 며칠 뒤에나 안다.

### 19-8. 턴을 **손님+글** 단위로 센다

팔자오빠에서 이런 일이 있었다 — 손님이 A 글에 생년월일을 적고 B 글에 와서 딴 걸 물었다.
봇은 B 글타래에 생년월일이 없는 걸 보고 **답을 안 하고 붙들었다.** 그게 맞는 판단이다.

타로에도 같은 자리가 있었다. `손님기록` 이 **글을 구분하지 않고 username 만 봤다.**

```
A글에 댓글 → 1턴, 카드 '연인'
B글에 댓글 → 기록상 2턴 → "아까 그 카드 그대로 보고 있어"
              그런데 B글타래엔 그 카드가 없다. 손님은 뭔 소린지 모른다
```

**아직 안 터졌다 — 글이 4개뿐이라 안 겹쳤을 뿐이다.** 글이 쌓이면 반드시 난다.

이제 **새 글이면 다시 1턴이고 카드도 새로 뽑는다.** 카드 씨앗에 글아이디를 섞어서
다른 글에서 같은 카드가 또 안 나온다. 옛 기록(글아이디 없음)은 예전 방식으로 센다 —
안 그러면 지금 손님들이 전부 1턴으로 되돌아간다.

### 19-9. 댓글을 못 보고 있던 것 둘 — **제일 급했던 버그**

**① `/replies` 는 대댓글을 안 준다**

```
/replies       남의 댓글 18개
/conversation  남의 댓글 23개
```

못 보던 5개가 하필 **되묻고 고쳐 말한 손님들**이었다. 대화가 이어지려는 제일 아까운 자리다.

```
@new.26_aa    고마웡..음,,,,무슨뜻인지 이해가 잘 안가는데..온다는걸까?
@hazel020725  아 재회아니고 새로운 인연궁금했어
```

팔자오빠는 같은 이유로 `/conversation` 을 쓰는데 서아만 안 고쳐져 있었다.
`/conversation` 은 **대화 나무 전체**를 준다 (깊이 0·1·2 전부, 깊이 제한 없음).

**② 25개에서 잘렸다**

`/replies` 는 기본 25개만 준다. 페이지를 안 따라갔다.
사장님 실측 **"1시간에 댓글 38개"** — 25개 벽에 바로 부딪히는 중이었다.
`끝까지()` 로 페이지 끝까지 따라간다.

### 19-10. API 를 때려도 안 죽게

| 구멍 | 고침 |
|---|---|
| `멈춤()` 이 `sys.exit` — 한 번 삐끗하면 **바퀴가 통째로 죽었다** | 429·5xx·끊김은 2·6·18초 쉬었다 세 번까지 다시. 세 번 다 실패해도 죽이지 않고 알린다 |
| 5분마다 글 10개를 다 읽어 **하루 3,100번** | 3일 지난 글은 건너뛴다 (`댓글붙는날 = 3`) |
| 사용률을 안 봤다 | `x-business-use-case-usage` 헤더를 읽는다. **80% 넘으면 그 바퀴를 쉰다.** 지금 2% |

### 19-11. 대기표를 셋이 덮어쓰고 있었다

`발행` · `줍기` · `채널관리` 가 같은 파일(`기록/답글대기.json`)을 **잠금 없이 통째로** 읽고 썼다.
바퀴는 5분마다 도는데 그 사이에 다른 놈이 쓰면 서로 날렸다.

> 22:31 채널관리가 답을 올림 → "1건 올렸다"
> 22:33 대기표를 보니 그 줄이 없음. **바퀴가 덮어썼다**

`대기표.py` 를 만들었다. **대기표를 고치는 길은 `대기표.합치기()` 하나뿐이다.**

- 잠금 파일 하나 (60초 넘게 잡혀 있으면 죽은 것으로 보고 뺏는다)
- **통째로 안 덮어쓴다.** 새 줄을 넣고 내가 고친 필드만 바꾼다
- 임시파일 → `os.replace` 라 쓰다 죽어도 표가 안 깨진다

### 19-12. 버려진 댓글 줍기 — `줍기.py`

거르는 기준은 세게 잡아야 한다. 스팸에 답글을 달면 계정이 위험하다.
그런데 세게 잡으면 **진짜 손님이 같이 걸린다.**

```
@zghj14l  스하리 완
          다음주에 전남친이 있는 술자리에 가면 재회가 가능할까요?
          → 버림: 품앗이·영업 댓글이다(스하리)
```

- 기준 자체는 `분류.볼만한가()` 에서 고쳤다 — **품앗이 말이 있어도 뒤에 질문이 남으면 본다**
- **이미 버려진 것**은 `줍기.py` 가 5분마다 회수한다. `안함`(기준에 걸림) + `실패`(검사 3번 못 넘김)
- 한 번 주운 건 `주웠나` 로 찍어서 또 안 줍는다

### 19-13. 채널관리 — 팔자오빠에 없는 것

사장님 지시: *"서아가 여자니까 캐릭이 채널 관리를 해야 하거든. 타로 관련이 아니라서 패스되는
댓글들, 이것들도 답글 달게 해야 해. **약간의 여성미, 약간의 댕청미.**"*

`채널관리.py` — **1시간마다. AI 안 쓴다.** 종류 7가지를 문장은행으로 답한다.

```
사과 → 무섭다 → 또올게 → 감사 → 칭찬 → 응원 → 인사
```

(사과가 감사보다 위다. *"미안 ... 그래도 고마워"* 는 사과로 받아야 한다)

결 잡은 것 셋 —
- **칭찬은 받되 자랑으로 안 받는다.** 카드 쪽으로 돌린다 ("실물이 더 예뻐")
- **미안하다는 말엔 내 탓으로 받는다** ("내가 먼저 물어봤어야 했는데")
- **댕청미는 억지 애교가 아니라** 자기도 놀랐다·부끄럽다로 낸다

`{카드}` 자리에는 손님기록에서 그 손님이 마지막에 받은 카드를 꺼내 쓴다.
**아무 데도 안 걸리면 안 답한다.** 채널 관리 댓글은 안 달아도 손해가 아니다.

### 19-14. 발행까지 걸리는 시간

| 턴 | 늦춤 | 왜 |
|---|---|---|
| 1~4 | 3~15분 | 파이썬이라 즉시 나오지만 봇 티가 난다 |
| **5~** | **50~70분** | AI 가 쓰는 깊은 대화 자리. 2분 만에 척 답하면 봇 티가 난다 |

바퀴를 따로 만들지 않았다. **`발행.py` 의 숫자 하나로 갈린다** (`깊은턴` · `깊은대화늦출분`).
사장님 지시: *"안 복잡하고 오류 안 나는 쪽."*

### 19-15. 값 (실측)

| | 토큰 | 시간 |
|---|---|---|
| 1~4턴 (조립) | **0** | 0.001초 |
| 5턴~ (AI) | 약 5,800 | 16~18초 |
| 채널관리 | **0** | 0.001초 |

**5턴까지 오는 손님이 드물어서 하루 값이 거의 0이다.** 2026-09-24 실측으로 나간 30건 중 3턴이 최대였다.

길이 실험도 했다. **하한이 길이를 정한다. 상한은 사고 막는 울타리일 뿐이다.**

| 시킨 범위 | 실제로 나온 길이 | 호출 | 토큰 |
|---|---|---|---|
| 200~480 | 188~216 | 8번 (절반이 걸려 재시도) | 약 38,000 |
| **100~400** | 156~179 | **4번** | **18,472** |
| 60~300 | 125~167 | 5번 | 23,579 |

AI 는 **시킨 범위의 아래쪽에 붙는다.** 480 근처는 한 번도 안 갔다.

### 19-16. 고친 파일과 백업 (백업 파일은 2026-09-25 전부 지웠다. 되돌릴 건 깃 이력)

| 파일 | 뭘 했나 | 백업 |
|---|---|---|
| `타로엔진.py` | 조사 `이랑` · `명사낱말()` · `절뽑기()` · digest 에 손님명사/손님절 | `.bak-2026-09-24-사람결전` |
| `조립.py` | 칸 11개 · 말결 4종 · 속마음 · 완충 · 마지막조립(3턴) · 턴 받기 | `.bak-2026-09-24-사람결전`, `.bak-말결전` |
| `풀이.py` | `AI안쓰는턴=4` · SYS_문맥 삭제 · 말투 물림 · AI 실패 폴백 · 글아이디 | `.bak-2026-09-24-5턴전`, `.bak-글단위전` |
| `말투.py` | **새 위치** (웹/말투.py 에서 올림). 웹·스레드 공용 | `.bak-2026-09-24-말결전` |
| `발행.py` | `/conversation` · 페이지 끝까지 · 재시도 · 사용률 · 3일컷 · 잠금 · 깊은턴 | `.bak-페이지전`, `.bak-튼튼전`, `.bak-잠금전`, `.bak-글단위전` |
| `분류.py` | 품앗이 말이 있어도 질문이 남으면 본다 | `.bak-2026-09-24-품앗이전` |
| `손님기록.py` | 턴을 글 단위로 | `.bak-2026-09-24-글단위전` |
| `자동.py` | 채널관리 붙임 · `--바퀴` 가 도움말만 찍던 것 고침 | `.bak-2026-09-24-채널관리전` |
| `테스트.py` | 분리검사가 카톡 폴더를 오진하던 것 고침 | `.bak-2026-09-24` |
| `데이터/반말뜻.json` | 정의 정방향 "정확히" → "그대로" (표시광고법 막힌말) | `.bak-2026-09-24` |
| `대기표.py` `줍기.py` `채널관리.py` | **새로 만듦** | — |

### 19-17. 내가 낸 사고 (같은 걸 또 밟지 마라)

| 사고 | 까닭 |
|---|---|
| `자동.py --바퀴` 가 **도움말만 찍고 아무것도 안 했다** | 채널관리를 `elif` 로 끼워 넣어 `--붙이기` 검사로 흘러내렸다 |
| 채널관리가 올린 답이 **영영 안 나갔다** | `바뀐것` 은 댓글아이디로 맞추는데 **방금 넣은 새 줄도 같은 아이디**라 "되살림" 으로 덮였다 |
| 조립 2·3턴이 **연달아 같은 결**로 나왔다 | 턴마다 쓸 수 있는 결 개수가 달라져 나머지 계산이 어긋났다. 목록을 고정하고 못 쓰는 결은 넘긴다 |
| 카드 뜻이 **5턴 내내 똑같이** 나왔다 | 같은 카드라 뜻도 그대로다. 1턴과 짚는 턴만 켠다 |
| `말투.py` 를 고치다 **SYS 닫는 따옴표를 같이 잘라먹었다** | 문자열이 안 닫혀 파일 전체가 깨졌다. 고칠 때 경계를 꼭 확인 |

### 19-18. 아직 안 된 것 (옛것 — 20-7 이 최신)

| # | 것 | 왜 급한가 |
|---|---|---|
| 1 | ~~서아는 전부 PC 에서만 돈다~~ | **20절에서 됐다.** R2 창고 + 깃허브 대타 + Cloudflare 워커. 비밀값 넣는 것만 사장님 몫 (20-6) |
| 2 | 프로필 링크 | 3턴 마지막이 "프로필에서 열어봐" 라고 하는데 프로필이 비어 있다. **지금은 헛말이다** |
| 3 | 결제 | 값을 안 정했다 |
| 4 | 팔자오빠 쪽 이식 | 서아에서 검증한 뒤 옮긴다는 게 사장님 방침. 사주 1턴 조립은 일간 10 x 약한오행 5 + 세운 십신 10 = 60문장이면 골격이 선다 |

---

## 20. 2026-09-25 새벽 — **기록은 R2, PC 꺼지면 클라우드가 대신 돈다**

사장님 지시 셋: ① 파일은 전부 클라우드로 ② PC 켜져 있으면 PC, 꺼지면 클라우드 **20분 텀** ③ 깃허브 호출보다 **클라우드 호출이 1번**.
그리고 ④ **기록 파일은 깃허브에 올리지 않는다** (계속 자라서 자리가 모자란다) ⑤ **다른 폴더와 합치지 않는다.**

### 20-0. 한눈에

```
PC 켜짐   작업 스케줄러 타로봇_바퀴 (5분)  ─┐
                                          ├─ 같은 R2 창고 seoa/ 를 본다 → 중복 없음
PC 꺼짐   Cloudflare 워커 seoa-wake (20분) ─┼→ 깃허브 액션 seoa.yml ─┘
          깃허브 자체 cron (20분, 몇 시간 늦음) ┘   심장박동 20분 넘게 없을 때만 돈다
```

| 것 | 어디 | 팔자오빠 것과 |
|---|---|---|
| 코드 | 깃허브 `koii0803/tarot-seoa` (master. 공개 전환은 20-8) | 저장소 다름 |
| 기록 (대기표·손님기록·발행기록·하루치·글창고·성적·토큰.log·자동기록) | R2 버킷 `babcheck-img` 의 **`seoa/`** | 접두어 다름 (팔자오빠 `threads-reply/`) |
| 깨우기 | 워커 `seoa-wake` → https://seoa-wake.bobomusic83.workers.dev | 워커 다름 (팔자오빠 `shorts-wake`) |
| 비밀값 | tarot-seoa Secrets (`SEOA_*`, R2 넷, 텔레그램 둘, 클로드) | 이름부터 다름 |
| 카드 그림 | 그대로 `tarot/` (11절) | — |

### 20-1. 창고 — `창고.py`

- **기록은 전부 여기다.** PC 에 `기록/` 폴더는 이제 없다 (웹 채팅 `웹손님.jsonl` 만 남았다 — 웹 서버는 PC 에서만 돈다)
- 파일마다 이름이 같다: `답글대기.json` `손님기록.jsonl` `발행기록.jsonl` `하루치.json` `글창고.json` `성적.csv` `토큰.log`(뒤 2000줄) `자동기록.txt`(뒤 400줄)
- 거기에 `잠금.json`(누가 돌고 있나) `심장박동.json`(PC 마지막 시각) `상태.json`(채널관리 마지막 시각)
- **R2 에 못 닿으면 아무것도 안 한다.** 텔레그램으로 알린다. 로컬로 대충 돌리면 PC 와 깃허브가 어긋나 같은 답이 두 번 나간다
- 한 프로세스 안에서는 읽은 걸 캐시한다 (손님기록을 바퀴마다 수십 번 읽는다). 바퀴는 잠금으로 한 번에 하나만 돌아서 안전하다
- 보는 법: `python 창고.py --확인` · `--목록` · `--보기 답글대기.json`
- ⚠️ 이 버킷은 **공개 버킷**(r2.dev)이다. 주소를 알면 `seoa/손님기록.jsonl` 도 읽힌다. 손님 아이디·댓글·우리 답이 들어 있다 (스레드에서 이미 공개인 것들. 생년월일 같은 건 없다). 비공개 버킷을 만들려면 S3 열쇠를 새로 받아야 해서 아직 안 했다 (20-7)

### 20-2. 바퀴 하나로 — `자동.py`

아침(하루치)·채널관리 일감을 **없애고 바퀴에 넣었다.** 셋이 따로 돌다 표를 날린 적이 있어서다 (19-11).
```
0 창고에 닿나 → 1 잠금 → 2 PC 면 심장박동 → 3 04시 지났고 오늘 하루치 없으면 짜기
→ 4 예약글 내보내기 → 답글 만들기 → 줍기 → 답글 내보내기 → 5 채널관리(1시간 지났으면) → 6 잠금 풀기
```
- `python 자동.py --바퀴` PC · `--바퀴 --대타` 깃허브 (심장박동 20분 넘게 없을 때만) · `--심장박동` 숫자 하나 · `--상태`
- 단계가 터지면 다음 단계는 돌고 **텔레그램으로 알린다.** 조용히 넘어가는 자리를 없앴다
- 작업 스케줄러에는 `타로봇_바퀴` 하나뿐이다. `--붙이기` 가 옛 둘(아침·채널관리)을 지운다
- 2026-09-24 23:20 PC 에서 한 바퀴 실제로 돌렸다: 심장박동·잠금·창고 쓰기 전부 됐고 답글 2건 나갔다

### 20-3. 깃허브 대타 — `.github/workflows/seoa.yml`

- 먼저 `자동.py --심장박동` 만 본다 (boto3 만 깐다, 20초). PC 가 살아 있으면 여기서 끝
- 심장박동이 20분 넘게 없으면 클로드를 깔고 `--바퀴 --대타`
- **R2 에 못 닿으면 빨간 불로 끝낸다.** 깃허브가 메일로 알린다. Secrets 가 비어 있어도 여기서 걸린다 (2026-09-24 23:22 실제로 빨간 불 확인 — Secrets 가 아직 비어서)
- 수동: `gh workflow run seoa.yml -R koii0803/tarot-seoa` (`-f force=true` 면 PC 살아 있어도 돈다. 같은 창고·잠금이라 중복은 안 난다)

### 20-4. 깨우기 워커 — `워커/`

- `seoa-wake`, 20분마다 `seoa.yml` 을 workflow_dispatch 로 깨운다. 깃허브 자체 cron 은 2~6시간씩 늦어서 2번이다
- 배포됨. 다시 배포: `워커/` 에서 `env -u CLOUDFLARE_API_TOKEN -u CLOUDFLARE_ACCOUNT_ID npx --prefix ../../saju-arcade wrangler deploy`
- 비밀값 `GITHUB_TOKEN` 하나 (워크플로 실행 권한). **아직 안 넣었다 → 20-6**

### 20-5. 같이 고친 것

| 파일 | 뭘 |
|---|---|
| `발행.py` | **조용한 버그**: 검사에 걸린 답글이 하나라도 있으면 `줄들` 이 없는 이름이라 NameError → "댓글 답글 만들기" 단계가 통째로 죽었다. 19절 뒤로 계속 그랬다. 고쳤다 |
| `발행.py` `하루치.py` | 올리자마자 그 줄부터 창고에 표시한다. 뒤에서 R2 가 삐끗해도 같은 답이 또 안 나간다. 표시를 못 하면 크게 알린다 |
| `대기표.py` | 파일 잠금 → 창고. 14일 지난 줄은 정리 (표가 계속 자라지 않게) |
| `서아토큰.py` | 깃허브에선 `SEOA_THREADS_TOKEN` · `SEOA_USER_ID` 환경변수를 먼저 본다 |
| `테스트.py` | 4) PC 기록 파일 쓰는 코드 남았나 5) R2 닿나 — 두 검사 추가. **전부 통과** |
| `비밀올리기.py` | 새로. PC 값을 tarot-seoa Secrets 로 (값은 안 찍는다) |
| 백업·찌꺼기 | `.bak-*` 전부, `__pycache__`, `깨진것`·`원본`·`전것`, 로컬 `기록/` — **지웠다** (깃 커밋 16bd156 에 옮기기 전 상태가 있다) |

### 20-6. 사장님이 할 것 (비밀값은 AI 가 못 넣는다 — 권한 막힘)

```
cd C:\Users\mjj\Desktop\사주팔자\타로봇
python 비밀올리기.py                       ← R2 넷·텔레그램 둘·서아 토큰을 깃허브 Secrets 로
claude setup-token                          ← 브라우저 뜨면 허용. 나온 토큰 복사
python 비밀올리기.py --클로드               ← 클립보드의 클로드 토큰을 Secrets 로
cd 워커
gh auth token | npx --prefix ../../saju-arcade wrangler secret put GITHUB_TOKEN   ← 워커가 깃허브를 두드릴 열쇠
```
확인: `gh workflow run seoa.yml -R koii0803/tarot-seoa` → 1분 뒤 `gh run list -R koii0803/tarot-seoa` 초록.
그다음 `curl https://seoa-wake.bobomusic83.workers.dev` → `token:true`.

### 20-7. 아직 안 된 것

| # | 것 | 왜 |
|---|---|---|
| 1 | ~~Secrets 넷 (20-6)~~ | 사장님이 넣었다 (R2·텔레그램·서아 토큰·워커 열쇠). **클로드 토큰만 남았다** — `claude setup-token` → `python 비밀올리기.py --클로드` |
| 2 | 깃허브 무료 시간 | `python 비밀올리기.py --공개` 한 줄이면 끝 (20-8). 그 전까지는 cron 2시간·워커 심장박동 확인으로 월 400분 안 |
| 3 | ~~비공개 버킷~~ | 대신 파일을 잠갔다 (20-8). 버킷은 그대로 공개지만 내용은 못 읽는다 |
| 4 | ~~서아 토큰 60일~~ | 바퀴가 알아서 늘린다 (20-8). 갱신이 막히면 텔레그램이 온다 — 그때만 `python 서아토큰.py --넣기` |
| 5 | 웹 서버·카톡 스킬서버 | HTTP 서버라 깃허브 액션으론 못 돈다. PC 전용 그대로 (카톡 인수인계 6절) |
| 6 | 프로필 링크·결제·팔자오빠 이식 | 19-18 그대로 |

### 20-8. 같은 날 밤 두 번째 — 구멍 막기 · 토큰 자동 갱신 · 공개 저장소 (사장님 지시)

| 것 | 뭘 했나 |
|---|---|
| **창고 잠금** | 공개 버킷이라 주소만 알면 읽히던 것 → `창고.py` 가 심장박동·잠금·상태 빼고 **전부 암호로 잠가서** 올린다 (`enc1:`). 열쇠는 `R2_SECRET_ACCESS_KEY` 에서 뽑는다 (새 비밀값 없음). 공개 주소로 읽으면 `enc1:Pa7p…` 만 보인다. `python 창고.py --목록` 이 잠겼나까지 보여준다 |
| **팔자오빠도 같이** | `쇼츠저장소/작업/답글창고.py` 에 같은 방식(코드는 따로, import 없음). 생년월일 든 `기록/날짜.json` 포함 11개 잠갔고 insta-shots2 에 올렸다. 사장님 "구멍 막아" 지시라 예외로 손댔다 |
| **토큰 자동 갱신** | `서아토큰.갱신()` — 만료 20일 전부터 하루 한 번 스레드 `refresh_access_token` 으로 60일 연장. 새 토큰은 창고 `토큰.json`(잠김)에 두고 PC 파일에도 적는다. **PC 와 깃허브가 창고 토큰을 먼저 본다** → Secrets 는 첫 씨앗일 뿐, 다시 안 넣어도 된다. 바퀴가 부른다. 실패하면 텔레그램 |
| **워커가 먼저 본다** | `seoa-wake` 가 R2(STORE 바인딩)의 심장박동을 읽어 PC 가 살아 있으면 깃허브를 안 두드린다. 못 읽으면 깨운다 (안 깨우는 쪽이 더 위험) |
| **저장소 공개 (사장님 몫)** | 비공개는 액션이 **실행마다 1분씩 올림 계산**이라 자주 두드리면 월 2,000분을 넘는다. 공개로 바꾸기 전에 카톡 비밀 경로가 든 옛 커밋을 밀어야 하는데 **AI 는 깃 역사 밀기(force push)가 막혀 있다.** → 사장님이 `python 비밀올리기.py --공개` 한 줄. 그동안은 워커가 심장박동을 먼저 보고, 깃허브 cron 은 2시간에 한 번만이라 월 400분 안이다. 실행 화면의 손님 아이디는 `@***` 로 가린다 |
| 남는 불이익 | 프롬프트·조립 문장·계정 조사 문서가 남에게 보인다. 비밀값·기록은 안 보인다 (Secrets·잠긴 창고). 팔자오빠 insta-shots2 도 이미 공개다 |

`python 테스트.py` 전부 통과. 워커 상태 `token:true, pc_alive:true`.

---

## 21. 2026-09-25 낮 — 1턴 답에 "기우는 방향" 한 줄, 같은 글 안 같은 줄 금지, 5턴 예시

**왜.** 첫 훅 글(2026-09-24 20시) 3시간 만에 댓글 78개. 전부 읽어 보니
- 손님 **6명이 "무슨 뜻이에요?"** 로 되물었다. 1턴 조립이 상징·뜻만 나열하고 **묻는 것에 기우는 방향이 없어서**다
- 한 글 밑 답글 45개에 "그쪽 얘기만 있고 니 얘기가 없네" 6번, "될지를 묻는데 하고 싶은지는 안 적었어" 6번. **구경꾼이 죽 읽으면 기계 티**
- (스하리 건 "알바중인게…" 는 버그 아님. 지워진 다른 손님 댓글에 단 답이 트리에서 밀려 보인 것)

**고친 것 (스레드 쪽만. `카톡/` 은 안 건드렸다 — 따로 살린다는 지시 그대로)**

| 어디 | 무엇 |
|---|---|
| `데이터/기울기.json` (신설) | 22장 × 정역 = 44칸. 될까·올까 물음에 카드가 **된다·안된다·아직** 어느 쪽으로 기우는지 한 줄씩 2벌. 전부 "기울어·쪽이야" 꼴 (단정 금지). 금지어·말투검사 통과 |
| `조립.py` | ① `_기울기줄()` — 물음꼴·미래꼴이면 뜻줄 다음에 붙는다. **1~2턴, 길어도 안 끈다** ② `고르기()` — `random.choice` 대신. `피할줄`(이 글에서 이미 나간 줄)에 없는 것부터 고른다. 짚기처럼 없어도 되는 칸은 다 썼으면 **비운다** ③ `쓴줄모으기(글아이디)` — 손님기록 + 대기표에서 그 글의 줄을 모은다 ④ 짚기 문장 5종을 각 2~4벌로, 뜻줄·뒤집기·자기얘기·모른다·단정·남겨두기·완충·여는줄 칸 늘림 |
| `풀이.py` | `한판()` 이 `조립.쓴줄모으기(글아이디)` 를 조립·마지막조립에 넘긴다. AI 세 번 실패 → 조립 대타에도 넘긴다 |
| `말투.py` | 5턴 SYS 끝에 **예시 5개** (어긋남·오래된 실·피한 것·고쳐 말한 것·말의 온도). 규칙은 못 따라와도 예시는 따라온다. 되물음 끝맺기에 **"둘 중 하나 고르게 던져라"** 추가 |

**확인한 것.** `python 조립.py --시험` 6건 다 기울기 줄 붙음 · `python 테스트.py` 전부 통과 · 같은 질문 손님 10명 × 5판에 겹친 줄 12개(거의 카드 뜻줄. 같은 카드라 어쩔 수 없음) · `풀이.한판()` 진짜 글아이디로 시늉 1건 통과.

**남은 것.** 손님이 "프로필 어디?" 하고 물었다 — 3턴이 "프로필에서 열어봐" 하는데 **프로필에 링크가 없다.** 사장님이 정할 것.

**21-2. 질문 하나에 카드 하나 (2026-09-25 낮, 사장님 지적 "매 턴마다 카드를 까는 거야?")**
전에는 턴마다 새 카드였다. 그러니 2턴이 "카드는 안 바꿨어" 해놓고 새 카드 얘기를 했고, 5턴부터 AI 가 매번 "오늘 카드는 …" 해서 카드 자판기였다. `풀이.카드씨앗()` 신설:
1~2턴 같은 카드 · 3턴 새 카드 뒷면 · 4턴~ 1턴 카드로 돌아옴 · 손님이 **유형이 다른 물음**을 새로 던질 때만 새 카드(`_새질문이냐`, haiku 안 부름).
AI 프롬프트에 "처음에 이미 보여준 카드다. 새로 뽑은 척 금지" 를 넣었다 (새 카드일 때만 "새로 뽑았다고 해도 된다"). 조립 4턴 여는줄은 "덮어둔 건 두고 첫 카드로 돌아올게". 10턴 시늉으로 확인 — 죽음 카드 하나로 10턴이 이어지고 AI 가 낫·강·흰 꽃을 턴마다 다르게 짚는다.

**21-3. 한 장 더 (2026-09-25 낮, 사장님: "5턴부터는 AI 가 제안도 할 수 있고 손님이 뽑자고 할 수도 있다. 왕복이 늘어야 스레드가 밀어준다. 어차피 5회니까 2장까지")**
`풀이.카드정하기()` 가 `카드씨앗` 을 대신한다. 4턴부터는 **기록에 적힌 마지막 앞면 카드로 고정**(`엔진.digest(고정=)`)하고, 새 카드는 세 길 — ① 손님이 "한 장 더·다시 뽑아" 하거나 내가 "뽑아볼까?" 한 뒤 "응" (`손님이더달래`) ② 딴 **가족**의 물음(마음/일/돈) ③ AI 가 `draw_more=true` 로 답하면 새 카드 쥐여 주고 다시 시킴. 프롬프트가 AI 에게 "제안(마지막 줄 '한 장 더 뽑아볼까?')" 과 "바로 뽑기" 두 길을 알려준다. **한 대화에 2장까지**(`더뽑기최대`). 스키마 키는 영문만 된다(한글 키 → API 400).
**그림은 카드를 새로 깔 때만** 붙는다 (사장님 지시: 1턴만, 2·3·4턴 안 붙임, AI 도 한 번만). 3턴 뒷면 그림도 이제 안 붙는다.

**21-4. 상징을 진짜 그림에 맞춤 (2026-09-25 낮, 사장님: "파이썬이 이미지 카드를 알고 짓거리는 거야?")**
`데이터/카드22.json` 의 symbolPoints 가 전통 라이더웨이트 그림 기준이었다 (죽음 = 흰 꽃·지는 해·강·문). 사장님 그림(검정·금·은)엔 그게 없다 — 손님이 그림 보면서 읽는데 "흰 꽃 거기가 먼저 보이더라" 하고 있었다. **22장 그림을 전부 열어 보고 그림에 진짜 있는 것으로 카드마다 5개씩 다시 썼다.** 옛 상징을 가리키던 카드별질문 41개는 지웠다(661→ 남음). 백업은 `%TEMP%\카드22_백업.json` · `카드별질문_백업.json`.
`데이터/기울기.json` 에 카드마다 `쉽게`(비유 없는 한 줄)를 넣었고, 조립에 **설명조립** — 손님이 "무슨 말이야·설명해줘·이해가 안 가" 하면 2턴부터 비유 없이 카드 이름·쉬운 뜻·기우는 방향으로 풀어준다 (@cola_184 실측). AI 턴도 같은 신호면 프롬프트에 "쉬운 말로 다시" 를 넣는다. 2턴 되물음이 1턴과 똑같이 나가던 것도 고쳤다(2턴 씨앗을 바탕#2 로, 되물음은 이미 나간 거면 같은 칸의 다른 물음).
**마이너 56장은 없다.** 그림이 22장뿐이라 22장으로 돈다. 78장으로 가려면 그림 56장이 먼저다(사장님 몫). 글 데이터는 그림만 있으면 만든다.

**21-5. 게시글 — 댓글·리포스트 기준으로 다시 짬 (2026-09-25 낮, 사장님: "게시글 쓰는 것도 봐줘. 다른 스레드에서 댓글이나 리포스트 많이 달린 기준")**
조사는 `데이터/스레드타로계정조사.md` **8부**. 결론: 답글은 무료 리딩·**번호 고르기**·질문 던지기, 리포스트는 **사람 행동 목록**. 카드 한장 글은 조회 200 에 좋아요 1 이라 뺐다.
- 글감 **번호** (풀이.번호글, AI 없음): 뒷면 넉 장 + 숫자 그림(`카드이미지/타로-번호4장.png` → R2 `tarot/pick4.png`, 주소표 `번호4장`, 그림올리기.번호그림만들기/파일올리기/번호그림주소). 손님이 "2" 라고 적으면 `풀이.한판(내글글=)` 이 번호 글인지 보고(`번호글이냐`·`번호뽑기`) 글아이디+번호 씨앗으로 정해진 카드를 `조립.번호조립` 으로 뒤집어 앞면 그림과 같이 답한다. 1턴으로 센다. 발행.댓글모으기가 `내글글` 을 같이 싣는다.
- 글감 **노하우** (AI, POST_GUIDE·노하우프롬프트·줄수한도 12): 증상축에서 상태 하나를 골라 그 상태인 사람들의 행동을 번호로 2~3개, 5~6줄에서 "근데" 로 끊고, 카드는 끝에 한 번, 마지막은 물음표.
- 하루치 자리: 08:00 번호 · 12:30 무료 · 17:00 노하우 · 21:30 무료. 내일(09-26) 새벽 4시 하루치부터.

**21-6. 클로드 토큰 Secrets 에 들어감 (2026-09-25 저녁).** `CLAUDE_CODE_OAUTH_TOKEN` 넣었다 → 깃허브 대타가 5턴 AI·하루치 노하우/무료 글까지 다 돈다. 20-6 의 "클로드 토큰만 남았다" 는 끝. 붙여넣기가 파워셸에서 계속 헛돌아 `비밀올리기.py --클로드` 는 클립보드에 없으면 직접 붙여넣게 묻고, 줄바꿈·공백은 지우고 본다. 결국은 `gh secret set CLAUDE_CODE_OAUTH_TOKEN -R koii0803/tarot-seoa -b "<토큰>"` 한 줄로 넣었다.

**21-7. 서아 말에서 뺀 것·넣은 것 (2026-09-25 저녁, 사장님 지시)**
- **기계·봇·자동·AI** — 계정사실의 "사람이 아니라 기계가 뽑는다" 삭제(스레드·카톡). 글·답글 금지어. 죽음 카드 상징 "기계 말"→"강철 말". 서아는 사람이다.
- **돈·무료·공짜·돈 안 받아** — 계정사실의 "돈을 안 받는다" 삭제. 글·답글 금지어. 돈 얘기 자체를 안 꺼낸다.
- **복채는 스하리** — 무료 리딩·번호 글에 "복채는 스하리" 한 줄. 스하리·복채는 금지어에서 뺐다(맞스하리·반하리·맞팔은 여전히 금지). 번호 댓글은 "스하리 완료 3번" 처럼 앞에 말이 붙어도 잡는다(짧은 댓글에 1~4 숫자 하나).
- 무료 글 줄수한도 4→10 (한 줄이 짧고 줄이 많은 게 실측으로 맞다).
- 프로필 소개의 "복채도 안 받아" 는 사장님이 직접 고칠 것.

---

## 22. 2026-09-25 밤 — 78장 전환 **끝** (사장님: 그림 안 보고 이름만 맞춰라)

**한 것 (2026-09-25 밤, 두 번째 세션)**
- `데이터/카드78.json` — 22장은 카드22.json 그대로(img 를 png 로만), 마이너 56장 추가 (ko `완드 에이스`·`컵 2`·`소드 페이지`·`펜타클 킹` 꼴, roman 빈칸, 상징 4개는 **정통 라이더웨이트 요소를 이름으로만** 씀 — 그림 안 봤다. 틀린 건 사장님이 그 카드만 짚어 준다).
- `반말뜻.json`·`기울기.json` 56장 손으로 채움 (AI 안 돌림, 토큰 0). `카드별질문.json` 은 **안 채웠다** — 마이너 56장은 엔진이 열린질문(유형x시제)으로 내려간다. 채우려면 `python 질문채우기.py --전부 --이어서` (sonnet 112번, 토큰 씀 — 사장님이 시킬 때만).
- `타로엔진.py` IMG_DIR→타로78장, 카드78, `% len(CARDS)`, selftest 78. `그림올리기.py` IMG_DIR·카드표·**접두어 `tarot78/`** (옛 `tarot/` 는 1년 캐시라 같은 이름으로 덮으면 옛 그림이 나올 수 있어 새 접두어). R2 에 78장+뒷면 올림, 주소표 갱신. 22장 옛 주소·`-역`·`번호4장` 은 옛 tarot/ 그대로.
- 테스트.py 67/67, 조립 --시험 마이너 카드 글 정상. 카톡(`카톡/`)은 **안 건드렸다** (스레드 글 작업만).
- **카드별질문.json 마이너 56장 손으로 채움** (유형 8 x 2줄 = 896줄, 사장님: "공식처럼 말고 머리 써서"). AI 안 돌림.
- 조립.py 조사맞추기: 숫자 이름 카드(컵 2 · 소드 10) 받침 처리 (`받침있나` 에 숫자 읽기 추가, 정규식에 0-9). 상징 이름이 다른 카드 상징 안에 통째로 들어가던 것 9곳 이름 바꿈(check_output 오탐 방지). 남은 겹침은 22장 것(운명의 수레바퀴·절제 vs 심판 '금빛 날개') — 사장님 문구라 안 건드림.
- 남은 것: 뒷면·번호4장 그림이 옛 검정·금 그림체 — 파스텔로 다시 만들지 사장님께 물을 것. 22장 상징(symbolPoints)은 옛 검정·금 그림 기준 그대로 — 파스텔 그림에 안 맞는 카드는 사장님이 짚어 주면 그것만 고친다.

## 23. 2026-09-26 — 카드 문법을 글에서 뺐다 (사장님: "사람들이 소드·컵·정역방향을 아니? 알아듣게 표현해")

- **글·답글에 카드 이름·완드/컵/소드/펜타클·정방향/역방향·메이저/마이너를 한 글자도 안 쓴다.** 그림이 붙어 있으니 글은 "그래서 지금 니 상황이 어떻다"로 바로 간다. 스레드 인기 타로 글 실측(심야타로·인연타로·비타민타로)도 카드 이름이 안 나온다. 사장님이 준 참고 글(정역=속도·방향, 수트=움직임·마음·머릿속·현실, 시종·기사·여왕·왕=태도)은 **방식만** 쓰고 문구는 안 베꼈다.
- `데이터/카드말.json` **78장 x 정역** 손으로 씀 (AI 안 돌림). 방향마다 뜻 3 · 그림 4(보이는 것→상황 한 문장) · 기울기 3+쉽게 · 되물음 유형 8 x 2. 검사기: 카드 문법 말 금지, 길이, 부호, 한 방향 안에서 뜻/기울기/쉽게끼리 8글자 겹침 금지. 생성 스크립트는 `%TEMP%\claude\...\scratchpad\ct_*.py` (임시. 파일이 진짜다).
- `타로엔진.py`: `CARD_TALK` 로드, `digest()` 가 한줄·되물음을 카드말에서 씨앗으로 고정해 뽑고 `뜻벌·그림벌·기울기벌·되물음벌` 을 fact 에 실어 준다. `check_output` 은 **뽑힌 카드 이름도** 걸고 `카드문법말`(정방향·역방향·완드·소드·펜타클·컵 ·똑바로 나왔) 도 건다.
- `조립.py`: 카드말 있으면 뜻줄 꼬리("딱 거기야") 없이 원문, 그림줄은 카드말 문장, 기울기·되물음은 방향별. 번호조립·설명조립에서 카드 이름 줄 뺌. 뒤집기줄 8벌을 "아직·속도·속에서 도는 중" 결로. 자기얘기줄은 카드 얘기 대신 "나도 이 자리에 있어 봐서". `_기울기줄` 은 뜻 3벌 전부와 겹치는 낱말 피함.
- `풀이.py`: AI 프롬프트에 이름 대신 `카드표기()`("이름 없음") · `방향표기()`(똑바로면 방향 말 금지, 거꾸로면 재료 한 줄 그대로만) · `공통규칙` 을 모든 글감·답글 프롬프트에 붙임. 노하우 "카드는 끝에 한 번"→"그림 한 번".
- 테스트 67/67 · 조립 --시험 · 게시글 3개(번호·무료·노하우) 확인. 옛 `반말뜻.json`·`기울기.json`·`카드별질문.json` 은 카드말 없을 때 뒤로 내려가는 예비로 남겨 둠.
- 남은 것: 뒷면·번호4장 그림 파스텔로 바꿀지(사장님) · AI 5턴 답글이 거꾸로 뜻을 지어내는지 실제 댓글로 볼 것 · 카드말 문구 중 사장님이 어색한 카드 짚어 주면 그 카드만 고침.

## 24. 2026-09-26 밤 — AI 규칙서 줄이고 "찍기" 넣음 (사장님: "조건이 너무 많아서 AI가 병신 됐네" / "타로는 추측성 발언이 있어야" / "카드가 이렇게 나와서 해석한 거다")

- 팔자오빠(@paljaoppa) 답글 실측: 손님 말 받기 → 성향·상대 마음 단정해 찍기 → "네 탓만은 아니야" → 시기 → 새 방향 물음. 심문 없음. 서아도 이 흐름.
- `말투.SYS` 170줄 규칙서(어긋남 짚어라·피한 걸 알아채라·예시 5개) → **45줄**: 서아 정체 3줄 + "타로는 찍는 거다" + 흐름 + 하지 마 5줄 + 잘 쓴 예 2개. "피한 걸 알아채라"가 심문조("대답 안 하고 넘어갔네")의 원인이었다. 웹 채팅도 같은 SYS 를 쓴다.
- `카드말.json` 에 **찍기** 78장 x 정역 x 3 (손님 성향 / 상대 마음 / 지금 상황). 엔진 `찍기벌` → 풀이 문맥프롬프트 "찍어 볼 것" + "지금 어느 때"(기울기 쉽게) → 조립 추측 자리에도 찍기.
- 검사 추가(풀이 `심문말`·`굳힌말`): "대답 안 하고·넘어갔네·피한 거야" / "지침·갇힘·머무름" 걸리면 다시 쓰게. 카드말에서도 "갇힘" 뺌.
- 10턴 시늉 전후 비교: 전엔 5~9턴 전부 "그것도 대답 안 하고" 심문. 후엔 "너 원래 애매한 거 못 참고 결론부터 내려는 타입이야 / 그림에 칼이 옆으로 기울었잖아 / 니 탓 아니야 / 지금은 정하는 때가 아니라" 흐름. 부호는 물음표만(사장님: 한국 여자들은 마침표 안 씀).
- 남은 것: 1~2턴 파이썬은 결이 되묻기면 찍기가 안 들어감(추측 결일 때만). 사장님이 원하면 1턴부터 찍기 켠다. 끝맺기 "남겨두기" 줄("아직 안 본 데가 있는데")이 가끔 뜬금없다.

## 25. 2026-09-26 새벽 — 실제 손님(jjjin_a_) 사고 → 사람처럼 이어가기 (사장님 지시 전부 반영, 푸시 완료)

- **사고**: 3턴에 손님이 마음을 털어놨는데("잘 지내라고 했는데 계속 신경 쓰이고…") 3턴 가림 규칙이 턴 수만 세서 "덮어둘게 프로필에서 열어봐"로 등 돌림. 4번째 말은 다른 글에 달아서 글아이디가 바뀌어 **1턴으로 되돌아감**.
- `손님기록`: 같은 손님은 글이 달라도 **12시간 안이면 이어서 센다**(`이어세는시간`). `마음말이냐`(25자 이상 또는 신경 쓰·연락하고 싶·보고싶·못 잊·힘들·모르겠… 낱말)면 **가림을 미루고** 아직 안 했으면 다음 짧은 물음에서 한다(`안내했나`). `방금뒷면` → 조립 여는줄 "덮어둔 건 그대로 두고 처음 카드로".
- 가림(마지막)은 **몇 턴이든 파이썬 조립**(5턴에 AI 가 "연락해 될까"로 깨진 뒤). 가림 앞에 관계 말이면 둘만줄 한 줄 먼저.
- **사람 관계 말**(남친·걔·그 사람·헤어·연락·썸·재회…): 1턴 둘만줄("이런 건 둘 아니면 아무도 몰라 절대 몰라"), 2턴~ 먼미래줄("이 사람이랑 결혼까지 갔는데 지금 이게 그대로면 후회가 막심… 홧김에 놓고 후회하는 것도 똑같이"), 카드마다 **두갈래** 한 줄("속상해서 한 말이면 A / 진짜 끝 앞이면 B", 78장x정역, `카드말.json` `두갈래`). 헤어질지 연락할지는 카드가 안 정한다 — AI 프롬프트에도 같은 지시(`풀이.문맥프롬프트` 관계 블록).
- **찍기**: 1~2턴·마음 말은 늘. 연애·재회·관계는 상대 마음, 일·돈·시험은 손님 성향, 불안·결정은 상황. 경우의 수(사장님: 메인 주제) — `상대벌`(+2) `돈벌`(+2), 되물음 연애·재회·돈 +2씩 (78장x정역). `조립._찍기줄`·AI "찍어 볼 것"이 쓴다.
- 돈 하소연(새·마이너스·빚…)엔 돈받기줄("돈은 니 통장이 제일 정확해 나는 흐름만 봐") 먼저. 짚기 "묻는 게 아니라 확인받고…" 8벌로.
- 한 답글 안 같은 뜻 두 줄 방지(`_겹침빼기`: 6글자 조각 또는 낱말 앞3자 2개 겹침). 남겨두기 끝맺음은 "안 본 데가 있는데" 대신 반쯤 꺼낸 말.
- 생성 스크립트(임시): `%TEMP%\claude\…\scratchpad\ct_*.py`, `patch*.py`. **파일이 진짜다.** 카드말.json 구조: slug → 정방향/역방향 → 뜻·그림·기울기·되물음·찍기·두갈래·상대벌·돈벌.

### (이전 기록) 78장 전환 시작만 했던 것

**사장님이 78장 그림을 다 만들었다.** `C:\Users\mjj\Downloads\타로카드_정리\` (00_Major_Arcana 22 · 01_Wands 14 · 02_Cups 14 · 03_Swords 14 · 04_Pentacles 14, jpeg). `_불량`·`_중복` 폴더는 쓰지 않는다. **새 그림은 파스텔·귀여운 그림체다. 옛 22장(검정·금·은)과 완전히 다르다.**

**한 것**
- `카드이미지/타로78장/` 에 78장 PNG 로 변환해 뒀다 (`00-fool.png` … `21-world.png`, `22-wands-ace.png` … `77-pentacles-king.png`, 폭 1024). 목록은 `타로78장/_목록.json`.
- 그 밖엔 **아무것도 안 바꿨다.** 봇은 아직 옛 22장 데이터·옛 그림으로 돈다.

**남은 것 (순서대로)**
1. `데이터/카드78.json` 새로 쓴다 — 22장은 카드22.json 꼴 그대로 옮기고, 56장을 더한다 (id 22~77, slug `wands-ace`·`cups-2`·`swords-page`·`pentacles-king` 꼴, ko 이름, keywords, symbolPoints 4~5개, upright/reversed_summary, img/imgPng/webPath). **그림 상징은 새 파스텔 그림 기준이어야 한다.** 22장 것도 다시 써야 한다 (지금 카드22.json 상징은 검정·금·은 그림 기준).
2. `데이터/반말뜻.json` · `기울기.json`(줄 2벌 + 쉽게) · `카드별질문.json` 을 78장으로. 반말뜻·카드별질문은 `반말뜻만들기.py`·`질문채우기.py` 가 AI 로 채우는 기계라 그걸 돌리면 된다 (22 로 박힌 숫자만 고치면).
3. `타로엔진.py`: `IMG_DIR` → 타로78장, `_load("카드22.json")` → 카드78, `draw()` 의 `% 22` → `% len(CARDS)`, selftest 의 22 → 78. `그림올리기.py`: IMG_DIR·카드표 바꾸고 `--올리기` 로 R2 에 78장 올린다 (주소표 갱신). 뒷면·번호4장 그림도 새 그림체에 맞춰 다시 만들지 사장님께 물을 것.
4. `조립.py` 카드줄·`풀이.py` 카드 이름 찾기(`c["ko"] == 이름`)는 그대로 돈다. `엔진.check_output` 의 "다른 카드 언급" 검사는 78장이면 "컵 2" 같은 짧은 이름이 보통 말과 겹치니 짧은 이름 규칙(뒤에 카드·정방향·역방향 붙을 때만) 이 잘 도는지 볼 것.
5. `python 테스트.py` · `조립.py --시험` · 10턴 시늉으로 확인 → 깃허브 푸시.

**⚠️ 이번에 크게 잘못한 것 — 다음 AI 는 절대 반복하지 마라.**
그림 78장을 확인하려고 Read 로 여러 장씩 반복 요청했다. 시스템이 "request limit" 으로 그림을 안 주는데 못 본 줄 알고 같은 그림을 대여섯 번 다시 요청했다. **주간 토큰 한도 10% 를 날렸다.** 사장님이 "이미지는 내가 다 확인했다, 너는 볼 필요 없다, 시킨 작업부터 해라" 고 했다.
→ **그림은 사장님이 보라고 할 때만, 한 번에 몇 장만, 안 보이면 바로 멈춘다.** 상징 문구는 그림 안 보고 그 카드의 정통 타로 그림 요소로 쓰고, 틀리면 사장님이 그 카드만 짚어 준다.

**이 세션에서 그 전에 끝낸 것은 21절(21-1~21-7) 에 전부 있다.** 전부 깃허브에 올라가 있고 봇은 그걸로 돌고 있다.
